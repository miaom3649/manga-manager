# 本地文件管理软件需求文档

项目名称：**H库**

## 1. 项目目标

开发一个以 Windows 电脑为文件存储端和主服务端的漫画与插画管理软件，用来索引、搜索和分类本机媒体文件。除 Windows 本机使用外，还支持手机通过浏览器与 Windows 配对后远程搜索、查看、阅读和分类。

用户当前持有的文件多数是以数字命名的 ZIP，例如：

- `459808.zip`
- `1234567.zip`

这只是当前文件的特征，不是软件的格式或命名限制。软件在 Ubuntu 环境中开发，通过 Windows 构建环境生成可执行程序，并在真实 Windows 电脑上完成最终验证。

## 2. 已确定的核心需求

- 所有漫画和插画进入同一个作品库。
- 主目录第一层的有效 ZIP 自动归类为“漫画”，专用插画目录中的可解码单张图片自动归类为“插画”。
- “漫画”和“插画”在界面中作为两个内置类型 Tag 使用，而不是两套独立作品库。
- 通过文件名查找对应文件；数字编号作为文件名时可以直接查询。
- 为漫画和插画作品添加一个标题、多个自定义标签（Tag）及个人星级评价。
- 点击标签筛选对应文件。
- 数据和漫画原文件保存在用户的 Windows 电脑上，不依赖第三方云端服务。
- 软件需要在 Windows 本机运行。
- 手机通过浏览器与 Windows 配对后，可以远程搜索、阅读和修改分类。

## 3. 初步技术方案

- Windows 主程序负责文件扫描、SQLite 数据库、系统托盘、配对和局域网服务。
- Windows 端采用原生桌面软件界面，不使用浏览器作为本机主界面。
- 手机端单独提供响应式网页，通过局域网访问 Windows 主程序。
- Windows 桌面界面与手机网页共用同一个本地数据库和服务接口，但针对桌面与触摸屏分别设计界面。
- 手机网页不直接扫描磁盘；文件系统访问、目录监控、ZIP 读取和缓存均由 Windows 主程序完成。
- Windows 端以标准安装软件交付，不依赖外部网站或云服务。
- 本地数据库：SQLite。
- Windows 程序打包：PyInstaller。
- Ubuntu 负责日常开发和大部分测试。
- Windows 自动构建负责生成 `.exe`。
- 真实 Windows 电脑负责最终验收。

技术方案会在需求确认后最终决定。

### 3.1 缓存策略

- 缓存用于封面缩略图、详情预览缩略图、GIF 动态预览及其他可重新生成的媒体数据，以避免反复读取 ZIP 或原图。
- 默认缓存上限为 1GB。
- Windows 设置中可选择 1GB、2GB、5GB 或不限，并显示当前缓存占用。
- 达到上限时按最久未使用顺序清理可重新生成的缓存；正在使用的缓存不得被删除。
- 提供“清理缓存”按钮。清理缓存不影响原作品、标题、Tag、封面选择、星级或阅读进度，需要时自动重新生成。
- 缓存设置属于 Windows 系统设置，手机端不单独管理。

### 3.2 外观主题

- Windows 桌面端和手机网页均支持浅色、深色、跟随系统三种外观模式。
- 首次使用默认“跟随系统”。Windows 端跟随 Windows 设置，手机网页跟随手机系统/浏览器设置。
- 每台设备独立记住主题选择。
- 软件主题主色为固定设计，不提供预设颜色或完整颜色选择器。
- 普通 Tag 的主题色、系统 Tag 强调色及灰色 Tag 在浅色和深色背景下都必须保持足够可读性。

### 3.3 界面语言

- 首版界面只提供简体中文，不提供语言切换。
- 文件名、作品标题、Tag、分组名和搜索输入仍支持完整 Unicode，包括中文、英文、日文和其他字符。

### 3.4 软件名称

- 用户可见名称为“H库”，用于 Windows 窗口标题、开始菜单、系统托盘、安装程序和手机网页标题。
- 内部包名、进程标识和构建产物可使用兼容性更好的英文标识 `HLibrary`，不影响界面显示名称。
- 首版使用临时占位图标，Windows 窗口、安装包和系统托盘先共用；正式图标留到界面原型或发布前确定。

## 4. 详细需求

### 4.1 文件存放方式

**问题 1：文件目前如何存放？**

- A. 全部在同一个文件夹中。
- B. 分散在一个主文件夹的多个子文件夹中。
- C. 分散在多个磁盘或多个互不相关的文件夹中。

用户答案：需要管理的文件位于同一个文件夹中；该文件夹的位置可以在软件内设置。软件需要记住用户选择的文件夹。

**问题 1.1：是否扫描子文件夹？**

用户答案：A。只收录所选文件夹第一层的文件，不递归扫描；该文件夹正常情况下不会包含其他目录，意外出现的目录直接忽略。

**文件夹更新方式**

- 软件启动时扫描所选文件夹。
- 软件运行期间自动监控文件夹，新加入、移出或变更的文件应及时反映到索引。
- 同时提供手动“刷新/重新扫描”按钮。
- 如果用户通过 Windows 资源管理器直接把新文件放入漫画主目录或插画目录，软件检测后只显示一条汇总通知，例如“发现 12 个新文件”，不逐文件弹窗。
- 用户点击汇总通知后打开新增文件列表。新文件已经位于目标目录，因此该页面不再复制文件，只负责格式校验和批量登记。
- 新增文件列表允许批量编辑标题、Tag 和漫画封面：可以为整批作品添加公共 Tag，也可以逐项修改标题、封面和独有 Tag。
- 合法新文件被检测后立即建立索引并出现在搜索结果中，不需要等待用户打开通知或完成批量编辑。
- 未编辑的漫画使用去扩展名文件名作为临时标题；ZIP 内可读图片按文件名自然升序排列，第一张作为默认封面，并自动添加“漫画”类型 Tag。
- 未编辑的插画使用去扩展名文件名作为临时标题、图片自身作为缩略图，并自动添加“插画”类型 Tag。
- 新增汇总通知和批量编辑页面只用于补充资料，不阻塞作品搜索、查看或阅读。
- 已收录文件在相同路径和文件名下被外部程序替换内容时，不自动决定是否继承资料，而是标记为“内容已替换，待确认”并生成可操作通知。
- 文件修改时间变化时先校验内容指纹；只有内容确实变化才判定为替换，单纯时间戳变化不得清除作品资料。
- 用户在 Windows 资源管理器中重命名已收录文件时，软件应识别为同一作品，不按删除加新增处理。
- 重命名后保留标题、Tag 关联、自定义封面、星级、阅读进度和缓存，只更新文件路径以及漫画编号或插画原文件名。
- “编号不可修改”仅指 H库 编辑界面不提供编号编辑；外部文件重命名可以改变软件显示的编号。
- 运行期间优先使用文件系统重命名事件识别；软件未运行时发生的重命名可结合内容指纹识别，避免仅依赖文件名。
- 替换通知提供两个选项：
  - “保留原资料”：保留标题、Tag 和星级，刷新文件内容与缓存；原自定义封面不存在时退回新 ZIP 自然排序后的第一张可读图片并提示；阅读进度超过新页数时调整到最后一页。
  - “当作新作品”：清除旧标题、Tag 关联、自定义封面、星级、阅读进度和旧缓存，再按新增作品流程使用默认资料加入并允许编辑。
- 用户尚未处理替换通知时，作品显示“待确认”状态并暂时禁止进入详情或阅读，避免旧资料与新内容混用。
- 同名替换通知允许在 Windows 软件和已配对手机的通知中心处理；任一设备作出选择后同步结果并结束其他设备上的待处理状态。
- 如果已索引的原文件被用户通过资源管理器删除或移走，软件显示汇总通知，例如“发现 12 个文件丢失”，不为每个文件分别弹窗。
- 用户点击汇总通知后打开缺失文件列表，查看具体文件名和原路径。
- 确认文件已从受管目录消失后，软件自动删除该作品的数据库记录，包括标题、Tag 关联、自定义封面选择、封面缓存和阅读进度。
- 缺失文件列表仅作为本次自动清理的结果报告，不保留可恢复的作品记录；软件不会执行额外的磁盘删除操作。

**通知中心**

- Windows 软件提供独立的通知页面/通知中心。
- 新增文件、缺失文件及其他需要告知用户的事件先短暂弹出提示，同时写入通知中心。
- 存在未读通知时，通知入口显示红点和未读数量。
- 用户打开通知或其详情后标记为已读；即时弹窗消失不等于已读。
- 已读和未读通知均最多自动保留 30 天，过期后自动清理。
- 用户可以手动删除单条通知或清空全部通知；删除和过期清理后同步更新未读数量。
- Windows 与该电脑的所有已配对手机共享通知列表和已读状态；任一设备标记已读或删除后，其他设备同步更新。
- 手机配对多台 Windows 电脑时，每台电脑的通知中心相互独立。
- 不发送 Windows 桌面/系统弹出通知。
- H库处于系统托盘时，如存在未读通知，托盘图标显示红点但不显示作品标题、封面或通知内容。
- 打开主窗口后，通知入口显示红点和未读数量；全部已读后托盘及应用内红点消失。
- 同一异常文件只在首次发现时通知一次；软件记录路径、大小和修改时间用于判断是否变化。
- 异常文件未变化时，后续启动或扫描不重复通知。
- 文件内容、大小或修改时间变化后重新校验；仍异常则产生新通知，变为有效文件则自动加入作品库并产生新增通知。
- 检测到一个或多个已收录文件在资源管理器中被重命名时，生成一条汇总通知，例如“发现 3 个作品文件被重命名”。
- 点击重命名通知后显示每项旧名称和新名称；重命名资料更新自动完成，不要求用户确认。
- 新增和缺失检测在软件启动扫描及运行期间实时监控时都生效。

**导入/上传入口**

- 上传不设置独立主页面；Windows 和手机的作品首页提供“上传”按钮，点击后打开上传卡片。
- 用户也可以将文件直接拖入 Windows 软件窗口，软件随即打开上传卡片并载入这些文件。
- 手机网页提供与 Windows 端相同的上传入口，可以从手机本地选择漫画 ZIP 或插画文件并上传到 Windows。
- ZIP 上传到漫画主目录；可成功解码的单张图片上传到固定“插画”子目录；其他格式拒绝上传。
- 选择或拖入文件后，先弹出上传页面，在其中编辑漫画标题和 Tag。
- 手机上传页同样支持标题、Tag 和漫画封面选择。
- 用户确认后才把文件复制到设定的漫画文件夹并建立索引；来源位置的文件保持不变。
- 上传开始后先把整批文件放入临时区域并完成格式、内容、空间和同名预检，不立即写入作品目录。
- 如果整批任务中存在一个或多个同名文件，只弹出一个“覆盖 / 取消”汇总对话框并列出全部冲突，不逐项询问，也不自动重命名。
- 选择“取消”时取消整批上传任务并清理全部临时文件，非冲突文件也不上传，原作品不受影响。
- 选择“覆盖”只代表允许流程继续，不会立刻覆盖磁盘文件；用户继续在上传卡片编辑和检查整批文件资料。
- 最终确认前允许返回上一步或取消整个任务，原作品文件及资料保持不变。
- 只有用户在最后一步点击“确定上传”后才提交整批任务。所有同名文件被覆盖，并使用上传卡片中为新文件填写的标题、Tag、封面和星级，不保留旧版本对应字段。
- 同名覆盖时清除旧阅读进度，新文件从头开始；最终提交失败并回滚时恢复旧进度。
- 覆盖项在上传卡片中留空时使用新作品默认值，不继承旧资料：标题使用去扩展名文件名，普通 Tag 为空，星级为未评价，漫画封面为 ZIP 内自然排序后的第一张可读图片，类型 Tag 自动添加，阅读进度从头开始。
- 最终确认页面明确显示本次将覆盖的作品数量和空字段默认规则。
- 最终提交采用事务式安全流程：覆盖前临时保护旧文件和数据库状态；全部文件与数据库更新成功后才清理保护副本，失败时回滚，避免部分覆盖。
- 局域网上传需要显示进度、成功或失败状态，并允许用户取消尚未完成的上传。
- Windows 和手机均支持一次选择多个文件批量上传。
- 批量上传时可以为全部文件统一添加一组公共 Tag，也可以继续逐个编辑各文件的标题、封面和独有 Tag。
- 公共 Tag 仅作为整批文件的初始预填；单个文件编辑时可以移除某个公共 Tag 或添加独有 Tag，最终以该文件确认时的 Tag 集合为准。
- 每个文件独立执行格式校验并在上传卡片中显示结果；不合格项必须从任务中移除或取消整批任务后，才能最终提交。
- 格式或内容不合法的文件在最终提交前明确列出并要求用户移除或取消任务；最终提交阶段不得静默跳过而形成与确认内容不一致的部分结果。
- 上传的 ZIP 如果不包含任何可读取图片，则拒绝上传并显示原因。

**问题 2：软件是否允许移动、重命名或删除原始文件？**

- A. 只管理索引，绝不修改原始文件（建议首版采用）。
- B. 允许移动和整理文件，但不允许删除。
- C. 允许移动、重命名和删除。

用户答案：A。软件只管理索引，不允许移动、重命名或删除原始文件。

例外：用户在设置中主动执行“更换漫画主目录”时，软件迁移当前整套作品库到新目录。除该明确操作外，软件仍不得移动或重命名原始媒体文件。

**更换漫画主目录**

- 更换目录不是切换到另一套独立资料，而是把当前作品库迁移到新位置。
- 迁移范围严格限定为数据库中已收录的作品文件：已收录漫画 ZIP 和已收录插画图片。
- 插画迁移到新主目录的固定“插画”子目录；新主目录创建新的“备份”子目录。
- 不迁移异常文件、未收录文件、未知子目录或旧“备份”目录；这些内容保留在原目录。
- 标题、Tag 关联、自定义封面、星级、阅读进度、通知和其他数据库资料保持不变，只更新文件路径。
- 执行前必须显示确认和迁移范围，并检查目标目录、磁盘空间和同名冲突。
- 迁移预检发现任意同名文件冲突时，取消整次迁移并列出全部冲突项；不覆盖、不跳过、不执行部分迁移。
- 同一磁盘优先使用文件系统移动；跨磁盘采用复制、完整性校验、确认成功后再删除旧文件的安全流程。
- 迁移中途失败时不得删除尚未验证的新旧文件，数据库仍应指向完整可用的一侧；提供清晰错误报告。
- 整次迁移成功后，删除由软件创建且已经为空的旧“插画”和“备份”子目录。
- 随后重新检查旧主目录：如果完全为空则删除旧主目录；如果还包含异常文件、未迁移文件、旧备份、未知子目录或任何其他内容，则保留目录和全部剩余内容。
- 迁移未完整成功时不执行上述旧目录清理。

### 4.2 文件名和文件格式

**问题 3：是否限制文件名或文件格式？**

用户答案已更新：媒体仍分为漫画与插画两种类型，但统一进入同一个作品库。漫画目录中主要为数字命名的 ZIP；另设插画目录，存放单张图片。插画不使用固定扩展名白名单，以文件能否被软件安全、成功解码为图像作为收录依据。

已确认目录结构：

```text
用户选择的主目录/
├── 459808.zip
├── 1234567.zip
└── 插画/
    ├── abc.jpg
    ├── illustration.png
    └── animation.gif
```

- 主目录第一层中内容有效的 ZIP 均作为漫画扫描，不限制文件名是否为纯数字。
- ZIP 文件名去掉扩展名后为纯阿拉伯数字时，该文本作为只读作品编号并保留前导零；包含任意非数字字符时作品编号留空。
- 非数字 ZIP 未填写标题时，仍使用去扩展名文件名作为临时标题，并允许按真实文件名搜索。
- ZIP 内必须至少包含一张软件可读取的漫画图片；没有可读取图片、压缩包损坏或无法打开时，不加入漫画索引。
- 漫画 ZIP 均不带解压密码。软件不提供密码输入或密码管理；遇到加密 ZIP 时视为异常文件，扫描时忽略、上传时拒绝并提示。
- 固定“插画”子目录中的图像文件作为插画扫描。
- 目标是覆盖所有主流静态和动态图像格式，包括但不限于 JPG/JPEG、PNG、GIF、WebP、BMP、TIFF、ICO、AVIF、HEIC；具体支持范围取决于随软件打包的图像解码器。
- 不承诺支持所有相机 RAW 或专业软件私有格式。无法解码、文件损坏或内容并非图像时，作为异常文件忽略。
- 音频、视频、文档及其他不支持的格式均视为异常或无关文件，直接忽略，不加入索引和搜索结果。
- 除固定“插画”目录外，其他子目录仍然忽略。
- 软件在主目录下创建固定“备份”子目录，用于存放配置和资料备份；该目录不参与作品扫描。

### 4.3 搜索方式

- 漫画和插画使用统一搜索结果，不再分别建立漫画库和插画库。
- “漫画”和“插画”作为内置类型 Tag，可以单独点击筛选，也可以不选类型而同时搜索两类作品。
- 文本搜索只匹配文件名/作品编号和用户标题，不匹配 Tag 名称。
- 文本搜索完成后可以继续选择类型 Tag、自定义 Tag 和星级条件进一步筛选；文本条件与筛选条件同时生效。
- 软件仍根据实际类型使用不同的详情页：漫画进入漫画详情和阅读器，插画进入插画详情。
- 未输入搜索内容时显示作品首页，并提供以下排序方式：最近添加、文件名/编号、标题、星级评价。
- 每台设备记住最后选择的排序方式；首次使用默认按“最近添加”倒序显示。
- 所有排序方式都支持正序和倒序：最近/最早添加、文件名或编号升序/降序、标题升序/降序、星级从高到低/从低到高。
- 每台设备同时记住最后选择的排序字段和排序方向。
- 搜索结果采用传统分页，不使用无限滚动或动态追加列表。
- 每次只查询和渲染当前页作品，提供首页、上一页、下一页、末页和指定页跳转。
- 显示当前页码、总页数和匹配作品总数。
- 封面优先读取本地缩略图缓存；GIF 只在当前页加载动态预览。
- 切换搜索条件、Tag 筛选或排序方式后回到结果第 1 页。
- 重新启动 Windows 软件或重新打开手机网页时，清空文本搜索、类型/Tag/星级筛选并回到第 1 页。
- 跨会话只保留按设备设置的排序字段与方向、主题和阅读模式等偏好，不恢复上次搜索结果状态。
- 在同一次会话中，从作品列表进入详情页或阅读器后返回列表时，必须恢复进入前的文本搜索、类型/Tag/星级筛选、Tag 匹配模式、排序、页码和列表滚动位置。
- 阅读器正常返回详情页，详情页再返回作品列表；如果提供直接返回列表的入口，也恢复同一列表状态。
- 如果用户编辑作品资料导致当前结果集合变化，则保留原查询条件并重新计算结果，不能显示已经不再匹配的旧数据。
- 每页固定显示 50 部作品，Windows 和手机均不提供修改每页数量的设置。
- 搜索结果采用单列列表，每行显示一部作品。
- 每行左侧显示封面，右侧显示标题、Tag 和星级评价；结果信息只读。
- 列表封面不显示可见边框、卡槽底色或裁剪框。图片保持完整原始比例，在固定的不可见布局区域内居中缩放，以维持条目高度一致。
- 图片使用轻微圆角。
- 图片以不裁剪为前提尽量放大并贴合不可见布局边界：至少宽度或高度有一项达到区域限制，另一方向居中，不让图片无故缩小漂浮。
- 图片本身下方添加柔和黑色投影，形成无框悬浮效果；投影强度需适配浅色和深色主题。
- 阴影沿图片实际轮廓绘制，不沿不可见布局区域绘制。
- 漫画条目在标题下单独显示一行纯作品编号，例如 `459808`，不添加“编号：”前缀。
- 插画条目在相同位置显示只读原文件名，也不添加字段前缀。
- 所有结果条目保持统一固定高度。
- Tag 在条目内使用单行横向滚动区域，不换行、不截断为“+N”，也不增加条目高度。
- Windows 支持横向滚轮、Shift + 滚轮或拖动滚动条查看；手机支持左右滑动查看。
- 结果条目中的 Tag 只展示，不可点击筛选或编辑；Tag 筛选只能在专门的筛选区域操作。
- Windows 在主列表左侧常驻显示筛选栏，所有 Tag 平铺展示，不显示分组标题。
- 手机端通过“筛选”按钮打开侧边抽屉，避免长期占用屏幕空间。
- Tag 筛选区域提供“管理”按钮；点击后打开 Tag 管理卡片，不设置独立的 Tag 管理主页面。
- Tag 管理卡片负责创建、改名、移动和删除 Tag/隐式分组，以及查看影响作品数量。
- 筛选栏/抽屉内提供独立的“搜索 Tag”输入框，只用于快速查找 Tag 或其隐式分组，不影响作品文本搜索。
- Tag 搜索支持中文、英文、日文及其他 Unicode 字符的部分匹配；搜索分组名称时显示该组全部 Tag，清空后恢复完整列表。
- Tag 搜索不区分英文字母大小写，并使用与作品搜索相同的 Unicode 规范化规则。
- “漫画”“插画”两个系统 Tag 固定显示在普通 Tag 之前。
- 其他 Tag 按名称升序进行 Unicode 感知的自然排序；不提供手动拖动排序。
- Tag 搜索结果仍按名称升序排列。
- 两端筛选内容一致，包括“漫画/插画”内置类型 Tag、普通 Tag、“任意匹配/全部匹配”模式和星级筛选。
- 筛选条件每次变化后立即刷新结果并回到第 1 页，不设置“应用筛选”按钮。
- 手机筛选抽屉在单次选择后保持打开，允许连续选择多个条件；用户主动关闭抽屉后查看结果。
- 提供“清除全部筛选”操作。
- Windows 和手机均使用单列布局，手机端根据屏幕宽度缩小封面和间距。
- Windows 双击整行进入对应作品详情页，避免单击选择时误触；手机端仍使用单击进入。

**星级评价**

- 漫画和插画均可设置 0～3 星评价。
- 0 星表示未评价；1 星表示“好”；2 星表示“非常好”；3 星表示“神作”。
- 星级显示在作品搜索结果条目和作品详情页。
- 搜索结果中的星级只显示，不能直接点击修改。
- 修改星级必须进入作品详情页并点击“编辑”，与标题、Tag 和封面一起在编辑状态中修改；保存后同步到 Windows 数据库和已配对设备。
- 星级是当前 Windows 个人实例拥有者设置的私人评价，不是大众评分；不提供评分人数、平均分、评论社区或多用户评分。
- 已配对手机只是查看和修改同一份个人评价。
- 星级可以作为筛选条件，支持筛选指定星级、某星级及以上，以及未评价作品。

**问题 4：文件名查询需要哪些匹配方式？**

- 精确搜索，例如输入 `459808`。
- 部分搜索，例如输入 `459` 显示文件名中包含该文本的文件。
- 一次粘贴多个文件名或编号并批量查询。

用户答案：选择 C。支持单个关键词的部分匹配，也支持一次粘贴多个文件名、编号或备注名称进行批量查询。

- 输入停止约 0.4 秒后自动执行搜索。
- 按 Enter 或点击“搜索”按钮时立即执行，不等待自动搜索延迟。
- 用户继续输入时取消尚未执行或已经过时的查询，避免旧结果覆盖新结果。
- 批量搜索使用英文逗号 `,` 分隔多个关键词，不使用空格或换行作为分隔符。
- 自动去除每个关键词首尾空白并忽略空项；每个关键词执行部分匹配，作品匹配任意一个关键词即可显示。
- 作品文本搜索不区分英文字母大小写，并进行 Unicode 规范化，使常见全角/半角字母和数字尽量等价匹配。
- 不提供简繁体自动转换、翻译、罗马音或拼音搜索。

**问题 4.1：是否支持备注名称（别名）搜索？**

用户答案：支持。用户可以在软件中为文件设置备注名称，例如为 `459808.zip` 设置 `abc`；这不会修改磁盘上的真实文件名。搜索备注名称时也要显示对应文件。备注名称允许中文、英文、日文、阿拉伯数字及其他任意 Unicode 字符。

备注名称与 Tag 是不同的数据：备注名称用于描述和搜索单个文件，Tag 用于分类和筛选多个文件。

**问题 4.2：一个文件可以有几个备注名称？**

用户答案：A。每个文件只有一个备注名称。由于该字段用于填写漫画作品名称，软件界面中将其称为“标题”。

### 4.4 标签功能

**问题 5：标签筛选采用哪种逻辑？**

文件同时有“已检查”和“项目A”两个标签时：

- 选择多个标签后，显示包含任意一个标签的文件（或）。
- 选择多个标签后，只显示同时包含全部标签的文件（且）。
- 两种模式都提供。

用户答案：C。提供“任意匹配（或）”和“全部匹配（且）”两种筛选模式，用户可以切换。

- “任意匹配/全部匹配”只作用于用户选择的普通自定义 Tag。
- 文本搜索、内置作品类型、星级条件、普通 Tag 筛选这几组条件之间始终使用“且（AND）”组合。
- 例如“漫画 + 校园或恋爱 + 2 星以上”只返回类型为漫画、具有校园或恋爱普通 Tag、并且至少 2 星的作品。
- 未选择类型 Tag 时同时包含漫画和插画；选择一个类型时只显示该类型；同时选择漫画和插画等同于不限制类型。

**问题 6：标签是否需要颜色、备注或分组？**

用户答案已更新：Tag 具有名称和隐式分组。筛选界面不显示分组层级，而是平铺全部 Tag；颜色不再用来区分具体分组。

- 所有已分组的普通 Tag 统一使用软件主题色。
- 未分组 Tag 统一显示为灰色。
- “漫画”“插画”两个系统 Tag 统一使用区别于普通 Tag 的系统强调色。
- 不为不同分组自动分配不同颜色，也不提供修改分组颜色的功能。
- 筛选栏只显示 Tag 名称和上述状态颜色，不显示“作者：XXX”或“类别：YYY”这样的显式分组标题。
- 软件内部仍保存每个 Tag 所属的分组。
- Tag 搜索同时匹配 Tag 名称和分组名称。例如搜索“作者”时，显示作者分组内的全部 Tag。

漫画与插画共用同一套自定义 Tag 和分组。“漫画”“插画”是额外的内置类型 Tag，由文件位置和内容自动赋予。这两个内置 Tag 完全只读：禁止删除、改名或手动添加/移除，也不受删除 Tag 分组操作影响。作品类型只能由文件所在位置和内容自动判断。

**创建 Tag**

- 首次使用时，除“漫画”“插画”两个内置类型 Tag 外，不预置普通 Tag 或分组。
- 筛选栏中提供“添加 Tag”卡片/入口。
- 创建 Tag 时填写名称，并可选择已有分组、保持未分组，或现场创建新分组。
- 创建新分组后，当前 Tag 作为已分组普通 Tag 使用软件主题色。
- 创建成功后，新 Tag 加入平铺 Tag 列表。

**Tag 管理保存方式**

- Tag 管理卡片内的操作逐项立即保存，不设置统一的最终“保存全部”步骤。
- 创建或改名在用户点击该项确认按钮后立即写入数据库。
- 移动 Tag 所属分组后立即写入数据库。
- 删除 Tag 或分组仍需按既定规则显示影响和确认；确认后立即写入数据库。
- 关闭 Tag 管理卡片不会撤销已经完成的操作。
- 未保存修改提示只针对当前仍在输入、尚未确认提交的内容。

**Tag 名称唯一性**

- 隐式分组名称全局唯一，不允许重复；创建或改名时校验并提示。
- 同一分组内不允许出现同名 Tag。
- 不同分组允许同名 Tag。
- Tag 名称全局没有重名时，界面只显示 Tag 名称。
- 同名 Tag 出现在不同分组时，所有重名项自动显示“分组：Tag”前缀形式，例如“作者：青山”“类别：青山”。
- 未分组 Tag 与其他分组重名时显示“未分组：Tag”。
- 分组前缀只用于显示和筛选区分，不改变 Tag 自身名称；重名消失后自动恢复为只显示 Tag 名称。
- 将 Tag 移动到另一个分组时，如果目标分组已有同名 Tag，则阻止操作并提示。

**删除 Tag 规则**

- 删除前显示当前使用该 Tag 的漫画和插画数量，并要求二次确认。
- 确认后只删除 Tag 及其与作品之间的关联记录。
- 不删除漫画或插画文件，也不删除作品记录、标题、封面、其他 Tag 或阅读进度。

**删除 Tag 分组规则**

- 删除包含 Tag 的分组时弹出选择对话框。
- 选项一：只删除分组，将组内全部 Tag 移入“未分组”。
- 选项二：删除分组及其中全部 Tag；执行前显示受影响的漫画和插画数量并二次确认。
- 选项二只删除 Tag 和作品关联，不删除任何作品记录或原文件。

### 4.5 漫画详情页与文件操作

**搜索结果展示形式**

搜索后显示一栏栏可能匹配的漫画结果。每条结果至少包含：

- 漫画封面：ZIP 内所有可读图片按文件名自然升序排列，默认读取排序后的第一张；也允许用户从压缩包内选择其他图片，并生成缩略图。
- 漫画标题：优先显示用户填写的标题；未填写时显示去掉扩展名的文件名，例如 `459808.zip` 显示为 `459808`。
- 漫画标签：显示用户为该作品添加的自定义 Tag。

ZIP 内图片通常使用连续数字命名，但前导零位数和起始数字不作固定要求。排序采用自然顺序，例如 `2.webp` 位于 `10.webp` 之前。封面仅从压缩包中读取，软件不修改原压缩包。为保证列表加载速度，软件建立本地封面缩略图缓存。

点击搜索结果后进入漫画详情页，不直接进入阅读器。详情页包含：

- 显示只读“作品编号”，取 ZIP 文件名去掉扩展名后的完整文本，例如 `459808.zip` 显示 `459808`。
- 仅纯数字文件名显示作品编号；非数字 ZIP 的编号区域为空。
- 作品编号不能在软件内编辑；保留前导零，不转换为整数。标题是独立的可编辑字段。
- 顶部展示当前封面；默认是 ZIP 内图片自然排序后的第一张，用户选择过其他封面时显示自定义封面。
- 详情页封面采用与结果列表一致的无框悬浮风格：完整比例显示，不裁剪，并添加柔和黑色投影。
- 封面下方展示可以左右滑动的横向图片预览栏。
- 预览栏本身是一个悬浮容器，具有圆角边框和柔和阴影。
- 最多五张预览图排列在同一个容器内部；超出可见宽度时在容器内左右滑动。
- 各预览图片本身不单独添加外框或悬浮阴影，避免视觉层级过多。
- 预览图使用统一显示高度；每张图片按原始宽高比等比例缩放，宽度随比例自然变化。
- 不裁剪、不拉伸；原图尺寸不一致时也缩放到相同可见高度，并在容器内纵向居中。
- 按住某张预览图时临时显示该图的大图预览：Windows 使用鼠标左键按住，手机使用手指按住。
- Windows 鼠标左键按下后立即显示大图。
- 手机手指保持基本不动 0.5 秒后显示大图；0.5 秒内发生明显横向移动时判定为滑动预览栏，取消大图触发。
- 大图在遮罩层中居中完整显示，保持比例、不裁剪；松开鼠标/手指、触摸取消或页面失去交互时立即关闭。
- 大图预览不进入阅读器，不提供单独关闭按钮。
- 禁用预览图默认拖拽、手机长按菜单等会干扰按住预览的浏览器行为。
- 预览栏不重复封面。以自然排序后的图片列表为准，从第 4 张开始每隔 3 张选取一张，即第 4、7、10、13、16 张。
- 预览图最多展示 5 张。作品较短时只展示实际存在的位置；不足 4 张时预览栏为空。
- 预览选择依据图片在排序结果中的位置，不要求文件名必须为 `004.webp` 等固定文本。
- 展示漫画标题和 Tag。
- 详情页 Tag 自动换行并完整显示，不使用横向滚动、截断或折叠。
- 查看状态下 Tag 只读；点击“编辑”进入编辑状态后才能添加或移除。重名 Tag 继续使用“分组：Tag”显示规则。
- 作品编辑状态中的 Tag 选择区提供搜索框，搜索同时匹配 Tag 名称和隐式分组名称。
- Tag 选择区提供“管理 Tag”按钮，可以在当前作品编辑界面上方打开 Tag 管理悬空卡片，并创建新 Tag 或分组。
- Tag 管理操作仍逐项立即保存；关闭管理卡片后返回原作品编辑状态，保留尚未提交的标题、封面、星级和 Tag 选择，并立即刷新可选 Tag 列表。
- 从作品编辑界面创建的新普通 Tag 自动加入当前作品的待保存选择；连续创建多个时全部自动选中。
- 新 Tag 自身在创建确认后立即保存，但与当前作品的关联只有在用户保存作品编辑后才正式写入；取消作品编辑不会删除已创建 Tag，只取消作品关联。
- 提供明确的“开始阅读”按钮；点击该按钮后从开头进入内置阅读器。
- Windows 点击“开始阅读”后关闭当前详情窗口，只显示阅读器；关闭阅读器或点击“返回详情”时重新打开对应作品的详情页。详情、阅读器和主窗口依次显示，不保留隐藏的模态窗口。
- 提供独立的“编辑”按钮，用于修改漫画标题和标签分类，不修改原文件名或 ZIP 内容。
- 所有现有作品资料修改均必须从详情页点击“编辑”后进行，包括标题、Tag、自定义封面和星级评价。搜索结果栏只展示，不提供快捷编辑。
- 编辑状态提供“保存”和“取消”；保存时统一提交本次修改，取消时不写入。
- 上传页面和详情页的编辑界面均提供“更改封面”功能。
- 更改封面时可以浏览 ZIP 内的全部图片并选择一张。软件只在数据库中保存所选图片在 ZIP 内的路径并更新封面缓存，不修改、替换或重命名 ZIP 内图片。
- 封面选择器使用缩略图网格，每页固定显示 50 张，显示每张图片在 ZIP 内的文件名。
- 提供首页、上一页、下一页、末页和指定页跳转，并显示当前选中状态。
- 点击图片只改变待选封面；点击“确认”后才保存并更新缓存，取消或关闭时保留原封面。
- Windows 与手机使用相同的封面选择逻辑，布局根据屏幕宽度调整列数。
- 封面选择界面复用阅读器的单页模式逻辑，不使用缩略图网格：一次显示一张图片并按自然顺序切换。
- Windows 使用左/上方向键查看上一张，右/下方向键查看下一张；手机点击左侧/右侧切换。
- 显示当前页/总页数并支持跳转指定页，提供“设为封面”和“取消”。
- 打开选择器时从当前封面图片开始；未自定义时从自然排序后的第一张图片开始。
- 只有点击“设为封面”才保存选择并更新缓存；取消或关闭不修改当前封面。
- 未手动选择时默认使用自然排序后的第一张可读图片。
- 点击封面或预览图片不进入阅读器；预览图片仅支持按住临时查看大图。

### 4.5.1 插画库与插画详情页

- 插画目录存放可解码的单张图片文件；扫描来源与 ZIP 漫画分开，但统一进入同一个作品库和搜索结果。
- 插画搜索结果形式与漫画结果一致，展示缩略图、用户标题和 Tag。
- 静态图片使用图片本身生成静态缩略图。
- GIF 在结果中显示动态缩略图；实现时需要限制预览尺寸和资源占用。
- 点击插画结果进入插画详情页。
- 插画详情页展示原图或适配窗口的图片、标题、Tag 和“编辑”按钮。
- 插画详情主图同样不显示可见边框，保持完整比例并添加柔和黑色投影。
- 插画详情页显示只读原文件名，不使用“作品编号”字段。
- 插画详情页不显示漫画使用的五张横向预览图，也不进入漫画阅读器。
- 插画详情页不提供放大、缩小、拖动或翻页功能。
- 插画标题和 Tag 的编辑规则与漫画一致，不修改原图片文件。

**问题 7：找到文件后需要哪些操作？**

- 用 Windows 默认程序打开文件。
- 在资源管理器中定位并选中文件。
- 复制文件完整路径。
- 如果文件是压缩包，查看其中的文件列表但不解压。
- 如果文件是压缩包，解压到指定目录。

用户答案：修改原方案。点击漫画搜索结果后先进入详情页，再从详情页进入软件内置漫画阅读器。

### 4.5.2 内置漫画阅读器

- 读取 ZIP 压缩包内的全部 WebP 图片并渲染显示。
- 原 ZIP 保持只读，不修改文件内容。
- 图片可以直接从 ZIP 读取或写入软件管理的临时缓存，无需永久解压到用户的漫画文件夹。
- 图片按文件名自然顺序排列，例如 `001.webp`、`002.webp`、`010.webp`。
- 采用纵向连续阅读模式，所有页面从上到下排列，使用鼠标滚轮滚动。
- 图片默认适配阅读区域大小并保持原始宽高比，即同时受可用宽度和高度限制，完整显示整张图片而不裁剪。
- 阅读器提供两种可以随时切换的阅读模式：
  - 纵向连续模式：全部页面从上到下连续排列，可以自由滚动，不强制吸附或准确停在某一页。
  - 单页模式：一次只显示一张图片，通过上一页/下一页操作切换。
- 两种模式共享同一个当前页。纵向模式根据视口中央主要显示的图片实时更新顶部页码和跳页框；切到单页模式时直接显示该页，切回纵向模式时也定位到单页模式的当前页。
- Windows 跳页数字框不显示上下调节箭头，用户直接输入目标页码。
- 两种模式均支持放大、缩小和拖动画面。纵向连续模式拖动时使用鼠标屏幕坐标计算滚动位移，滚动导致图片控件位置变化时不得出现卡顿、抖动或断续。
- 每次进入阅读器时缩放重置为“适配大小”；不按设备或作品保存缩放比例。
- 用户在当前阅读会话中仍可自由缩放和拖动，打开另一部漫画时不继承缩放。
- Windows 端支持界面按钮、鼠标和键盘操作；手机网页支持触摸手势。具体快捷键和手势在界面设计阶段确定。
- 每台设备分别保存最后一次选择的阅读模式。首次使用默认纵向连续模式；用户切换后，该设备下次打开阅读器自动沿用，无需重复设置，直到再次切换。
- 单页模式在 Windows 端使用鼠标滚轮翻页：向上滚动返回上一页，向下滚动前往下一页；也可使用 `↓` 或 `→` 前往下一页，使用 `↑` 或 `←` 返回上一页。
- 手机单页模式点击屏幕左侧返回上一页，点击右侧前往下一页。
- 点击屏幕下侧显示或隐藏阅读器工具栏；屏幕上侧不承担翻页操作。
- 拖动和双指缩放优先识别为手势，不应误触翻页。
- Windows 阅读器顶部使用应用内标题栏：左侧仅显示左箭头返回详情按钮，中间显示作品标题，不显示右上角关闭按钮。
- 阅读器底部工具栏包含：当前页/总页数、连续/单页模式切换、放大、缩小、恢复适配大小和跳转指定页；不显示上一页、下一页或全屏按钮。
- Windows 阅读器刚打开时标题栏和底部工具栏同时显示。鼠标远离窗口顶部和底部约 5 秒后，标题栏向上、工具栏向下同时滑出隐藏；鼠标靠近窗口顶部或底部任一侧时，两栏一起滑回显示，使用同一个隐藏计时。
- 页码输入框拥有输入焦点时，上下栏保持显示，即使鼠标已经离开也不得隐藏；输入框失去焦点且鼠标仍远离顶部和底部后，重新等待完整 5 秒才允许两栏一起滑出。
- 阅读器保留清晰的可见窗口边框；拖动顶部标题栏使用系统级窗口移动，可在桌面内稳定调整窗口位置，双击标题栏可最大化或恢复。
- 手机工具栏位于底部，由点击下侧唤出，并在 5 秒无操作后自动隐藏；手机同样不显示全屏按钮。
- Windows 可使用标题栏左箭头、Esc 或 Alt+F4 关闭阅读器，关闭后均恢复详情页。
- 手机发生触摸、缩放、模式切换或工具栏操作时重新开始 5 秒计时；底部点击不得同时触发翻页。
- 软件保存每部漫画上次观看的页面或滚动位置。
- 阅读进度保存在 Windows 数据库中，并在电脑和所有已配对设备之间共享；以最近一次正常退出该漫画阅读器时的位置为准。
- 阅读过程中只在当前设备内存中更新临时位置，单页翻页、纵向滚动、跳页及模式切换均不得写入数据库。
- 使用“返回详情”、Esc、Alt+F4 或手机阅读器返回按钮正常退出时，保存一次最终当前页；连续模式同时保存该页内相对位置。
- 程序崩溃、被强制结束、浏览器异常关闭或页面被强制刷新时，本次尚未退出的临时位置不保存，继续保留上一次正常退出时的历史进度。
- 连续模式进度记录当前主要可见图片编号及图片内相对位置，以适应不同设备屏幕尺寸。
- 每次进入阅读器仍默认从开头显示，不自动跳转到历史位置。
- 如果存在历史进度，进入阅读器时显示“回到上次观看位置”按钮；按钮默认显示 5 秒，点击后跳转至保存的位置，超时后自动隐藏。
- “上次观看位置”在本次打开阅读器时立即保存为固定快照；按钮显示期间产生的新浏览进度不得改变其跳转目标，因此无论当前页位于历史页之前还是之后都必须正常跳转。
- Windows 按钮宽度和高度必须完整显示“回到上次观看位置”，不得截断文字。
- 不设置“已读/未读”状态。

### 4.6 手机浏览器远程访问与配对

- 漫画原文件永久存储在 Windows 电脑上，不把整套作品库永久复制到手机；单部漫画阅读时允许创建可自动清理的临时离线缓存。
- Windows 软件运行时同时提供远程访问服务。
- H库 每次启动时自动开启手机局域网访问服务，不提供额外的“手机共享”开关。
- 首次配对后的手机以后直接使用已保存凭证连接，不需要每次扫码；H库 在主窗口或托盘运行时均可连接，彻底退出后不可连接。
- 手机配对后，可以完成与 Windows 端相同的搜索、详情查看、阅读、标题编辑和 Tag 分类操作。
- 手机端采用响应式网页应用，不开发独立 iOS App。
- iPhone 使用 Safari，Android 使用 Chrome 等现代浏览器；其他同一局域网设备也可通过浏览器访问。
- 网页可添加到手机主屏幕，以获得接近独立 App 的启动体验。
- 网络范围：仅限 Windows 电脑与手机连接同一个 Wi-Fi/局域网，不支持互联网异地访问。
- Windows 软件彻底退出、电脑休眠或网络断开时，手机无法访问；主窗口关闭但程序仍在系统托盘运行时，手机可以继续访问。
- 软件由用户手动启动，不提供开机自启动功能。
- 关闭主窗口时软件缩小到 Windows 系统托盘，后台服务继续运行，手机仍可访问。
- 用户通过托盘菜单选择“退出”后，程序和手机访问服务才完全停止。
- 上传、主目录迁移或其他文件任务正在进行时选择“退出”，必须弹出两个明确选项：“等待任务完成后退出”或“取消当前任务并退出”。
- 选择等待时程序保持托盘运行，任务完成后自动安全退出；选择取消时先安全停止任务、清理未完成临时文件或回滚未完成迁移，再退出。
- 关闭主窗口进入托盘不会暂停或取消正在进行的任务。
- 局域网访问使用 HTTP，不启用 HTTPS，也不要求手机安装本地证书。
- 所有远程接口仍必须验证已配对设备的高强度随机凭证；未配对设备不能搜索、查看、阅读、上传或修改资料。
- 服务仅监听本地局域网，不设计公网暴露或端口映射功能。
- 软件应提醒用户只在可信的私人 Wi-Fi 中启用手机共享，不建议在公共 Wi-Fi 使用。

**手机临时离线阅读缓存**

- 手机打开一部漫画时，将该漫画全部页面下载到浏览器临时存储，允许断开 Wi-Fi 后继续阅读。
- 下载显示进度，并允许已下载前几页后边读边继续下载剩余页面。
- 无论漫画是否看完，只要退出该漫画阅读器就立即删除这部漫画的手机临时缓存；下次打开时重新从 Windows 下载。
- 如果浏览器异常关闭或崩溃导致未正常清理，下一次打开 H库 手机网页时自动清理遗留的阅读会话缓存。
- 手机浏览器缓存不是永久离线收藏；操作系统或浏览器可能在存储空间不足时自行清理。
- 如果手机空间不足或浏览器首次拒绝缓存整部漫画，先自动清理一次 H库 在该浏览器中创建的旧缓存和异常遗留临时数据，然后自动重试当前漫画的整部缓存。
- 自动清理仅限 H库 自己创建的浏览器缓存，不得删除其他网站数据、App 数据、照片或手机文件。
- 清理后第二次仍无法缓存整部漫画时，才降级为在线逐页加载，并提示“手机存储空间不足，断网阅读不可用”，但不阻止阅读。
- 降级后已成功加载的页面在当前会话仍可查看，退出时统一清理。
- 手机退出阅读器时若 Windows 暂时不可连接，将最终位置保存为小型待同步记录；该记录不属于漫画图片缓存，退出阅读器时不删除。
- 重新连接对应的 Windows 电脑后自动上传待同步进度，Windows 确认保存成功后才删除手机记录。
- 待同步进度按 Windows 电脑和作品分别标识，切换其他已配对电脑时不得错误同步。
- 离线进度与 Windows 当前进度冲突时保留更靠后的阅读位置，不允许同步导致进度倒退。
- 单页模式比较页码；连续模式先比较主要可见图片编号，同页时比较图片内相对位置。
- 离线记录同时绑定作品内容指纹；作品文件已被替换为新内容时不得把旧离线进度套用到新作品，应丢弃该进度并通知用户。

**手机端权限范围**

- 手机允许：搜索、排序、筛选、查看详情、漫画阅读、上传漫画/插画、编辑作品标题/Tag/封面/星级、管理普通 Tag 与分组、查看和处理通知。
- 手机禁止：更换 Windows 漫画主目录、恢复配置备份、撤销或管理其他配对设备、彻底退出 Windows 后台服务。
- 上述限制必须由 Windows 后端接口强制执行，不能只在手机界面隐藏入口。
- 手机“设置”页只提供主题模式、当前连接电脑、切换已配对电脑、当前设备名称和断开当前电脑。
- 手机执行“断开当前电脑”时，删除本机保存的该电脑凭证并通知 Windows 撤销本设备；再次连接必须重新扫码。
- 目录、备份、已配对设备管理、扫描和托盘服务等系统设置只在 Windows 端显示。

**配对流程**

- Windows 软件显示包含局域网访问地址的二维码和配对码。
- 配对码不按时间自动过期，只在当前配对页面打开期间有效。
- 关闭配对页面后，当前二维码和配对码立即失效；下次重新打开配对页面时生成全新的二维码和配对码。
- 同一次打开配对页面期间，可以使用该配对码连续配对多台设备。
- 手机首次扫描二维码并完成确认后，Windows 将该设备记为已配对设备。
- 手机浏览器安全保存配对凭证；以后通过浏览器收藏或主屏幕快捷方式进入时自动识别，通常不需要重新扫码。
- Windows 端需要提供已配对设备列表，并允许撤销某台设备的访问权限。
- 手机首次配对时允许填写自定义设备名称，例如“我的 iPhone”。
- 已配对设备列表显示设备名称、设备类型和浏览器、首次配对时间、最近访问时间，以及“撤销访问”按钮。
- 撤销后，该设备已保存的凭证立即失效，必须重新扫码配对才能再次访问。
- 允许配对多台手机或浏览器设备，并允许多台设备同时访问。
- 所有设备共用 Windows 端的漫画索引、标题、标签和阅读进度数据；修改以 Windows 数据库为准。
- 同一台手机可以分别与多台 Windows 电脑配对，并保存多个电脑入口。用户可选择当前访问哪台电脑。
- 每台 Windows 电脑拥有独立的名称/标识、漫画文件、数据库、配对码和已配对设备列表，电脑之间不自动同步数据。
- 手机只能访问当前已开机、正在运行软件且处于同一 Wi-Fi/局域网中的已配对电脑。
- 如果电脑的局域网地址发生变化，应提供重新获取访问地址或二维码的快捷方式。

### 4.7 数据规模和性能

**问题 8：大概有多少个文件？总容量大约是多少？**

用户答案：当前约 146 个漫画文件，总容量约 2.7GB。该数字仅作为开发和性能测试基准，不作为软件上限。

实现要求：文件数量和总容量均动态统计，不写死限制；扫描采用增量索引，封面和预览图使用缓存，阅读图片按需加载，避免一次性把所有漫画内容载入内存。

未来数据规模不确定。设计目标暂定为至少 10,000 部作品时仍可正常搜索和筛选；这属于性能目标，不是硬性容量限制，实际容量取决于电脑存储空间。

### 4.8 使用和分发

**问题 9：软件由谁使用？**

- 只在自己的一台 Windows 电脑上使用。
- 自己在多台电脑上使用。
- 分发给其他人使用。

用户答案：主要供用户本人使用。安装包可以分享给其他有需求的人，但每个人都以独立的个人实例使用，不涉及多人共享同一漫画库。每台 Windows 软件只管理本机文件和数据。

**问题 10：希望使用绿色免安装版，还是标准安装包？**

用户答案：A。提供标准 Windows 安装包，包括开始菜单入口、可选桌面快捷方式和卸载功能。

程序、用户数据库、缓存和配对信息应分开存放；安装、升级或卸载程序时不得修改漫画原文件。是否在卸载时保留或清除用户数据，应由用户选择。

- 首版不提供联网检查更新、自动下载或自动安装更新。
- 用户通过手动运行新版安装包覆盖升级；升级必须保留数据库、设置、配对信息和原作品文件。
- 设置页显示当前软件版本。

### 4.9 配置与资料备份

- 备份包含标题、自定义 Tag 与分组、作品 Tag 关联、自定义封面选择、个人星级评价、阅读进度、通知记录、软件设置和配对资料。
- 备份不包含漫画 ZIP 和插画原图。
- 支持每日自动备份，也支持用户手动创建备份和恢复备份。
- 备份文件存放在用户选择的漫画主目录下的固定“备份”子目录。
- 自动备份文件名包含“自动”标记和创建时间，只保留最近 5 份成功的自动备份。生成并校验新备份成功后，才删除更旧的自动备份文件。
- 手动备份文件名包含“手动”标记和创建时间，不计入自动备份的 5 份限制，也不会被自动清理；由用户自行删除。
- 当天第一次启动软件时创建当天的自动备份。
- 如果软件连续运行多天，则每天凌晨 2 点创建自动备份。
- 凌晨 2 点电脑处于休眠状态时，在电脑唤醒且软件恢复运行后补做当天备份。
- 如果某天整天没有运行软件，则不为该日补做备份。
- 同一个自然日最多生成一份自动备份。
- 用户选择恢复备份时必须先弹出确认框，显示所选备份的类型和创建时间，并说明恢复会覆盖当前管理资料但不会修改漫画 ZIP 或插画原文件。
- 用户确认后，软件先创建一份文件名带“恢复前”标记和时间的保护备份；保护备份成功并校验后才执行恢复。
- “恢复前”保护备份按手动备份处理，不计入 5 份自动备份限制，也不自动删除。
- 如果保护备份创建失败，则取消恢复，保持当前数据不变。

## 5. 首版建议范围

首版建议先实现：

1. 选择一个扫描目录。
2. 扫描主目录第一层的有效 ZIP 漫画，以及固定“插画”子目录内可解码的图片；其他内容忽略。
3. 按文件名搜索。
4. 创建标签并为文件添加或移除标签。
5. 使用一个或多个标签筛选。
6. 通过漫画详情页进入内置阅读器；插画在详情页查看，不提供资源管理器定位。
7. 保存本地数据库并支持备份。
8. 生成标准 Windows 安装包。

### 5.1 主导航结构

- 主导航只保留“作品”“通知”“设置”三个入口。
- Windows 可使用左侧主导航；手机可使用底部导航或适合小屏的等效形式。
- Tag 管理通过作品页筛选栏中的“管理”按钮打开 Tag 管理卡片。
- 上传通过作品首页“上传”按钮或 Windows 文件拖放打开上传卡片。
- 作品详情页和漫画阅读器从作品列表进入，不作为主导航入口。
- Windows 和手机的 Tag 管理卡片、上传卡片均采用居中的悬空模态卡片，不跳转到独立页面，手机端也不强制全屏。
- 打开卡片时背景显示半透明遮罩；卡片内容较多时在卡片内部滚动。
- 关闭卡片后恢复原作品页面及其搜索词、筛选条件、排序和页码。
- 点击遮罩、右上角关闭按钮或 Windows 端按 Esc 都会尝试关闭悬空卡片。
- 卡片没有未保存修改时直接关闭；存在未保存修改时弹出“放弃未保存修改？”确认框。
- 用户选择继续编辑时返回卡片，选择放弃时关闭并丢弃本次未保存修改。

### 5.2 首次启动向导

- 软件尚未配置漫画主目录时，首次启动自动显示设置向导，不直接进入空白首页。
- 用户选择或新建一个漫画主目录。
- 软件检查固定“插画”和“备份”子目录；缺少时自动创建，已存在时直接使用，不覆盖其中任何文件。
- 完成目录设置后执行首次扫描，显示发现的漫画、插画和异常/忽略文件数量，然后进入作品首页。
- 手机配对不包含在首次向导中，用户之后可在设置页主动完成。
- 主目录配置完成后，正常启动不再显示首次向导。

## 6. Windows 验收重点

- 64 位 Windows 10 和 Windows 11 是否能正常安装、启动和卸载。
- 中文界面、中文标签和中文路径是否正常。
- 不同盘符及外接硬盘路径是否正常。
- 文件移动、丢失或新增后能否正确更新。
- 大批量文件扫描和筛选是否流畅。
- 软件关闭并重新打开后标签是否保留。
- `.exe` 是否被安全软件误报或拦截。

## 7. 需求确认记录

> 本节仅记录讨论演变，包含后来被覆盖的旧方案，不作为实现依据。开发、测试和验收一律以第 1～6 节正文中的最终规则为准。

- 2026-08-06：只需管理一个文件夹；文件夹路径由用户在软件内选择和设置。
- 2026-08-06：软件不得移动、重命名或删除原始文件，只能读取文件信息、管理索引和标签。
- 2026-08-06：更换主目录是只读规则的唯一例外；该操作迁移当前整套作品库并保留全部管理资料，跨磁盘时先复制校验再清理旧文件。
- 2026-08-06：主目录迁移只移动已被软件收录的漫画和插画作品文件；异常/未收录内容、未知目录及旧备份不移动。
- 2026-08-06：主目录迁移遇到任意同名冲突时整次取消并显示冲突列表，不覆盖或部分迁移。
- 2026-08-06：迁移成功后清理软件创建的空子目录；旧主目录完全为空时删除，否则保留所有未迁移内容。
- 2026-08-06：收录设定文件夹内的所有文件，不限制文件格式、扩展名或命名规则。数字命名的 ZIP 只是当前文件集合的特征。
- 2026-08-06：漫画与插画改为统一作品库，并共用自定义 Tag；“漫画”“插画”作为内置类型 Tag 用于筛选，详情页仍按实际类型区分。
- 2026-08-06：首页提供最近添加、文件名/编号、标题和星级排序，按设备记住最后选择，首次默认最近添加。
- 2026-08-06：所有排序字段均支持正序和倒序，并按设备记住排序字段与方向。
- 2026-08-06：取消无限滚动，结果改为传统分页；仅加载当前页，并支持首页、上一页、下一页、末页和指定页跳转。
- 2026-08-06：每页固定显示 50 部作品，Windows 与手机一致。
- 2026-08-06：结果采用单列列表，每行左侧封面、右侧标题/Tag/星级，点击整行进入详情页。
- 2026-08-06：结果条目在标题下显示只读编号/原文件名；漫画只显示纯数字编号，不加“编号：”前缀。
- 2026-08-06：列表封面、漫画详情封面和插画详情主图采用无框悬浮样式，完整比例显示并添加适配主题的柔和黑色投影。
- 2026-08-06：悬浮图片使用轻微圆角，并在不裁剪前提下尽量贴合隐形布局边界，阴影跟随图片实际轮廓。
- 2026-08-06：结果条目高度固定；Tag 采用单行横向滚动展示全部内容。
- 2026-08-06：结果条目中的 Tag 仅展示，不可点击；筛选统一在专门的 Tag 筛选区域完成。
- 2026-08-06：Windows 使用左侧常驻筛选栏，手机使用筛选抽屉；两端筛选能力一致。
- 2026-08-06：Tag 筛选区域提供独立的 Tag 搜索框，不影响作品搜索。
- 2026-08-06：筛选条件变化后立即刷新并回到第 1 页；手机抽屉保持打开，另提供清除全部。
- 2026-08-06：漫画和插画支持 0～3 星评价：1 星“好”、2 星“非常好”、3 星“神作”；结果栏和详情页均显示并支持星级排序。
- 2026-08-06：星级为用户自己的私人评价，不是大众点评；支持指定星级、至少某星级和未评价筛选。
- 2026-08-06：搜索结果中的资料均为只读；现有作品的标题、Tag、封面和星级必须进入详情页点击“编辑”后修改。
- 2026-08-06：只扫描所选文件夹第一层；不扫描子文件夹，遇到目录时忽略。
- 2026-08-06：启动时扫描文件夹，运行期间自动监控变化，同时提供手动刷新。
- 2026-08-06：通过资源管理器直接新增文件时自动弹出作品登记窗口；已索引文件被删除或移走时自动弹出文件缺失提示。启动扫描和运行期间均检测。
- 2026-08-06：直接新增文件改用汇总通知显示数量；点击后进入新增文件列表，批量编辑标题、Tag 和封面。
- 2026-08-06：新文件检测后立即使用默认标题、封面和类型 Tag 加入作品库；是否处理新增通知不影响搜索和阅读。
- 2026-08-06：同名文件内容被替换时改为待确认通知；用户选择保留并刷新资料，或清除旧资料后当作新作品。处理前禁止打开。
- 2026-08-06：同名替换的保留/新作品选择可在 Windows 或手机通知中心处理，结果跨设备同步。
- 2026-08-06：外部重命名已收录文件时识别为同一作品，保留全部资料，只更新编号/文件名和路径；软件内仍禁止修改编号。
- 2026-08-06：增加通知中心；即时提示短暂显示，通知历史保留，未读时显示红点和数量，打开详情后标记已读。
- 2026-08-06：通知保留 30 天后自动清理，同时支持手动删除单条或清空全部。
- 2026-08-06：Windows 与已配对手机共享通知及已读状态；不同 Windows 电脑实例的通知互相独立。
- 2026-08-06：不使用 Windows 桌面通知；未读状态通过托盘图标红点和应用内红点/数字提示。
- 2026-08-06：同一异常文件未变化时只通知一次；文件发生变化后重新校验并按结果再次通知或加入作品库。
- 2026-08-06：外部重命名生成汇总通知，详情显示旧名称和新名称；资料保留和编号更新自动完成。
- 2026-08-06：支持每日自动备份及手动备份/恢复；备份存入漫画主目录的“备份”子目录，不含媒体原文件，仅保留最近 5 份成功备份。
- 2026-08-06：自动备份和手动备份在文件名中标记区分；仅自动备份保留最近 5 份，手动备份不限数量且不自动删除。
- 2026-08-06：自动备份在当天首次启动时执行；连续运行时每天凌晨 2 点执行，休眠错过后唤醒补做；未运行的日期不补备份，每日最多一份。
- 2026-08-06：恢复前必须确认，并自动创建不限期保留的“恢复前”保护备份；保护备份失败时不得执行恢复。
- 2026-08-06：缺失文件使用一条汇总通知显示总数；点击后查看具体缺失文件列表，不逐文件弹窗。
- 2026-08-06：文件缺失后自动删除对应作品的全部数据库资料和缓存；缺失列表仅作为清理报告，不保留待恢复记录。
- 2026-08-06：Windows 端支持按钮选取或拖放文件进入上传页面，用户可在正式导入前编辑标题与 Tag。
- 2026-08-06：上传采用复制方式，保留来源文件不动。
- 2026-08-06：批量上传同名冲突使用一个整批“覆盖 / 取消”对话框；取消终止全部，覆盖仅允许继续编辑，最终确认时才事务式覆盖并应用新资料。
- 2026-08-06：上传覆盖同名作品时清除旧阅读进度；事务失败回滚时恢复原进度。
- 2026-08-06：覆盖项空字段使用新作品默认值，不继承旧资料；最终确认显示覆盖数量和默认规则。
- 2026-08-06：手机网页提供与 Windows 相同的上传机制；ZIP 进入漫画目录，图片进入插画目录，并支持编辑标题、Tag 和漫画封面。
- 2026-08-06：上传支持批量选择，可给整批文件统一添加公共 Tag，并可逐个设置标题、封面和额外 Tag。
- 2026-08-06：批量公共 Tag 可在单个文件中移除或补充，最终按各文件自己的 Tag 集合提交。
- 2026-08-06：ZIP 内没有任何可读取图片时拒绝上传；目录扫描遇到此类 ZIP 时忽略，不加入漫画库。
- 2026-08-06：漫画 ZIP 不加密；软件不实现压缩包密码功能，加密 ZIP 作为异常文件处理。
- 2026-08-06：插画结果使用图片自身作为缩略图，GIF 显示动态缩略图；插画详情页显示图片、标题、Tag 和编辑按钮，不显示漫画预览栏。
- 2026-08-06：插画详情页不提供缩放、拖动或翻页；漫画阅读器需要提供放大、缩小、拖动和翻页等控制。
- 2026-08-06：漫画阅读器可切换纵向连续与单页阅读；连续模式不强制按页吸附，单页模式一次只显示一页。
- 2026-08-06：每次进入阅读器均恢复适配宽度，不保存或继承缩放比例。
- 2026-08-06：阅读模式按设备持久记忆；修改一次后，该设备后续阅读自动沿用，直到再次切换。
- 2026-08-06：单页阅读时，电脑用下/右方向键下一页、上/左方向键上一页；手机点击右侧或下侧下一页、左侧或上侧上一页。
- 2026-08-06：手机单页操作最终改为左侧上一页、右侧下一页、下侧唤出工具栏，上侧不翻页；拖动/缩放不触发翻页。
- 2026-08-06：阅读器工具栏提供返回、页数、模式、缩放、适配、跳页和全屏；Windows 常显，手机自动隐藏。
- 2026-08-06：手机阅读器工具栏无操作 5 秒后隐藏，发生阅读或工具栏交互时重新计时。
- 2026-08-06：每部漫画的最新阅读进度在 Windows 和所有已配对设备之间共享；阅读模式偏好仍按设备独立保存。
- 2026-08-06：单页模式翻页后立即保存；连续模式在画面位置稳定 1 秒后保存，不要求手指或鼠标松开。
- 2026-08-06：Windows 端采用原生桌面软件界面；手机端保留响应式网页。两端共用 Windows 数据库与服务，但不强制复用同一套界面。
- 2026-08-06：目录结构确认为“主目录第一层存放 ZIP 漫画，固定插画子目录存放单张图片”；音视频等其他格式及其他子目录全部忽略。
- 2026-08-06：插画不限定扩展名，以能否成功解码为图像作为收录依据；覆盖主流格式，无法解码或损坏的文件忽略。
- 2026-08-06：手机网页只承担远程界面，不直接扫描磁盘；Windows 本地主程序承担扫描、监控、ZIP 读取和缓存。
- 2026-08-06：搜索支持部分匹配和多个关键词批量查询，查询范围包含真实文件名与备注名称。
- 2026-08-06：文本搜索支持约 0.4 秒防抖自动查询，也支持 Enter 或搜索按钮立即查询。
- 2026-08-06：批量搜索使用英文逗号分隔，关键词之间采用任意匹配逻辑。
- 2026-08-06：文本框只搜索文件名/编号和标题；Tag、类型与星级通过专门筛选区域叠加过滤。
- 2026-08-06：作品和 Tag 搜索不区分英文大小写并兼容常见全角/半角文本，不做简繁转换或翻译。
- 2026-08-06：文件可设置不影响原文件名的备注名称；备注名称支持中文、英文、日文、数字及其他 Unicode 字符。
- 2026-08-06：软件定位为本地漫画作品管理器；每个文件代表一部漫画，数字文件名通常是作品编号。
- 2026-08-06：每部作品只有一个备注名称，界面中称为“标题”；Tag 作为漫画的分类标签。
- 2026-08-06：多标签筛选同时提供“任意匹配”和“全部匹配”模式。
- 2026-08-06：普通 Tag 内部使用可切换的任意/全部逻辑；文本、类型、星级与普通 Tag 各组之间始终使用 AND。
- 2026-08-06：新会话清空搜索和筛选并回到第 1 页，仅保留排序、主题、阅读模式等设备偏好。
- 2026-08-06：同一会话从详情页或阅读器返回列表时恢复完整搜索、筛选、排序、页码及滚动位置；资料变化时按原条件重新计算。
- 2026-08-06：更新 Tag 展示：分组改为隐式属性，同组 Tag 自动共用分组颜色，未分组为灰色；筛选栏平铺 Tag，不显示分组标题。
- 2026-08-06：Tag 搜索同时匹配 Tag 名称和隐式分组名称；初始不预置普通 Tag，通过添加卡片创建 Tag、选择或新建分组。
- 2026-08-06：分组颜色默认自动分配，但允许用户修改；同组全部 Tag 随分组颜色一起变化。
- 2026-08-06：再次更新颜色规则：颜色不再对应具体分组；已分组普通 Tag 使用主题色，未分组 Tag 为灰色，两个系统 Tag 使用另一种强调色，不提供分组颜色设置。
- 2026-08-06：Windows 和手机均支持浅色、深色、跟随系统三种主题，按设备记住，首次默认跟随系统。
- 2026-08-06：软件主题主色固定，不提供用户自定义；Tag 状态色随浅色/深色主题适配可读性。
- 2026-08-06：首版界面仅提供简体中文，但用户内容和搜索完整支持 Unicode。
- 2026-08-06：软件命名为“H库”，内部工程标识暂用 `HLibrary`。
- 2026-08-06：首版使用占位图标，正式图标留到原型或发布前确定。
- 2026-08-06：缓存默认上限 1GB，Windows 可选 1GB/2GB/5GB/不限，并显示占用、支持安全清理和按最久未使用自动淘汰。
- 2026-08-06：主导航简化为作品、通知、设置；Tag 管理由筛选栏“管理”按钮打开卡片，上传由首页按钮或 Windows 拖放打开卡片。
- 2026-08-06：Windows 和手机均使用居中悬空卡片承载 Tag 管理与上传，关闭后保留原页面状态。
- 2026-08-06：悬空卡片可通过遮罩或关闭操作退出；有未保存内容时必须确认是否放弃。
- 2026-08-06：Tag 管理操作逐项确认后立即保存，不采用整张卡片统一提交；关闭卡片不撤销已完成操作。
- 2026-08-06：系统 Tag 固定在最前，其他 Tag 按名称升序自然排序，不支持手动排序。
- 2026-08-06：隐式分组名称必须全局唯一，支持 Unicode，并参与 Tag 搜索。
- 2026-08-06：首次启动提供主目录设置向导，自动创建缺少的插画/备份子目录并首次扫描；手机配对留在设置页。
- 2026-08-06：删除 Tag 前提示受影响作品数量；确认后仅删除 Tag 及关联，不删除任何作品或原文件。
- 2026-08-06：删除非空 Tag 分组时，可选择把组内 Tag 移至“未分组”，或在确认影响数量后连同组内 Tag 一并删除。
- 2026-08-06：不同分组的同名 Tag 仅在发生重名时显示“分组：Tag”前缀；无重名时隐藏分组，未分组重名项使用“未分组”前缀。
- 2026-08-06：“漫画”和“插画”两个内置类型 Tag 禁止删除，不受普通 Tag 或分组删除操作影响。
- 2026-08-06：内置类型 Tag 也禁止改名和手动修改；上传及扫描时由文件位置和内容自动分配作品类型。
- 2026-08-06：“漫画”和“插画”内置 Tag 禁止改名，但允许用户在作品编辑页手动修改分类。
- 2026-08-06：删除 Tag 分组时，组内 Tag 全部移至“未分组”，作品分类关系保持不变。
- 2026-08-06：搜索结果采用漫画卡片/条目形式，展示压缩包内第一章 WebP 封面、用户填写的漫画标题和自定义标签。
- 2026-08-06：`001.webp` 是默认封面；上传页和详情编辑页可浏览 ZIP 内全部图片并指定其他封面。选择仅保存到数据库，不修改 ZIP。
- 2026-08-06：封面选择器采用每页 50 张的缩略图网格，显示文件名并支持翻页/跳页；确认后保存，取消不改变封面。
- 2026-08-06：封面选择器采用单页阅读模式，一次查看一张、支持翻页和跳页；从当前封面开始，确认后才保存。
- 2026-08-06：修改原交互：点击搜索结果先进入详情页，再由详情页进入阅读器。
- 2026-08-06：阅读器采用纵向连续滚动模式，页面按文件名自然排序并默认适配窗口宽度。
- 2026-08-06：保存上次阅读位置，但重新打开仍从开头开始；有历史进度时显示持续 5 秒的恢复位置按钮，不设置已读状态。
- 2026-08-06：详情页顶部展示 `001.webp` 封面；横向预览栏最多展示 `004`、`007`、`010`、`013`、`016` 五张实际存在的 WebP 图片。
- 2026-08-06：五张预览图放在同一个带圆角边框和阴影的悬浮横向容器内，图片本身不各自添加外框或阴影。
- 2026-08-06：预览图统一高度、宽度按原图比例变化，不裁剪或拉伸。
- 2026-08-06：按住预览图时显示居中完整大图，松开立即关闭；不进入阅读器，不显示额外关闭按钮。
- 2026-08-06：手机按住预览图 0.5 秒触发大图，提前横向移动则作为滑动；Windows 鼠标按下立即触发。
- 2026-08-06：详情页 Tag 自动换行完整展示，查看时只读，编辑状态下才可修改。
- 2026-08-06：作品编辑时可打开 Tag 管理悬空卡片创建 Tag/分组；关闭后保留作品编辑现场并刷新 Tag 列表。
- 2026-08-06：作品编辑中创建的新 Tag 自动选中；Tag 本身立即创建，作品关联随作品编辑保存，取消编辑不删除新 Tag。
- 2026-08-06：详情页通过“开始阅读”按钮进入阅读器；另设“编辑”按钮修改标题和 Tag 分类，封面与预览图不作为阅读入口。
- 2026-08-06：未填写漫画标题时，使用去掉扩展名的真实文件名作为临时标题；详情页仍显示真实文件名以便核对编号。
- 2026-08-06：漫画详情显示从 ZIP 文件名得出的只读作品编号并保留前导零；插画详情只显示只读原文件名。
- 2026-08-06：有效 ZIP 无论是否数字命名都收录；只有纯数字文件名显示作品编号，非数字 ZIP 编号留空但仍可按文件名搜索。
- 2026-08-06：详情页不提供资源管理器定位或复制文件路径功能。
- 2026-08-06：当前数据规模约为 146 个文件、2.7GB，仅作为测试基准；软件动态处理容量和数量，不设置固定上限。
- 2026-08-06：未来规模不确定；暂以 10,000 部作品可正常搜索和筛选作为设计目标，而非固定上限。
- 2026-08-06：增加手机浏览器远程端；文件保留在 Windows，Windows 提供服务，配对后的手机可搜索、阅读和分类。
- 2026-08-06：远程访问仅限同一 Wi-Fi/局域网，定位为个人使用，不提供公网访问。
- 2026-08-06：手机端采用响应式网页，不开发独立 iOS App；兼容 iPhone Safari、Android Chrome 及其他现代浏览器。
- 2026-08-06：H库 启动即自动提供手机连接服务，不设置共享开关；首次配对后免重复扫码，彻底退出时服务停止。
- 2026-08-06：手机首次扫描 Windows 端二维码完成配对；配对凭证会被记住，下次可通过收藏或主屏幕入口快捷连接。Windows 端可撤销已配对设备。
- 2026-08-06：配对码在配对页面关闭前持续有效，不按时间过期；关闭并重新打开配对页面后生成新码并使旧码失效。
- 2026-08-06：已配对设备列表暂定显示自定义名称、设备/浏览器、首次配对时间、最近访问时间和撤销按钮。
- 2026-08-06：手机局域网访问使用 HTTP 和配对凭证，不使用 HTTPS；仅面向可信私人 Wi-Fi，并限制为局域网访问。
- 2026-08-06：手机打开漫画时整部临时缓存以支持当前会话断网阅读；退出阅读器即清理，不论是否看完，异常遗留缓存下次启动清理。
- 2026-08-06：手机整部缓存首次失败时先清理 H库 自有旧缓存并重试一次；再次失败才降级在线逐页加载并提示断网不可用。
- 2026-08-06：手机断网进度单独暂存，重新连接对应 Windows 后自动同步并在成功后清理，不随图片缓存删除。
- 2026-08-06：离线与在线进度冲突时保留更靠后位置；内容已替换时不应用旧离线进度。
- 2026-08-06：手机可管理作品和普通 Tag，但更换目录、恢复备份、配对设备管理和退出服务仅限 Windows 端，后端强制限制。
- 2026-08-06：手机设置页仅保留主题、电脑切换、设备名称和断开功能；系统级设置仅在 Windows 显示。
- 2026-08-06：允许多台设备配对及同时访问，所有设备共用 Windows 端数据。
- 2026-08-06：手机可分别配对多台 Windows 电脑并选择访问；各电脑实例及数据库完全独立，不进行跨电脑同步。
- 2026-08-06：安装包可以分享，但每位使用者均为独立个人使用，不设计多人账户或公共漫画库。
- 2026-08-06：Windows 端采用标准安装包，提供快捷入口和正常卸载；程序数据与漫画原文件隔离。
- 2026-08-06：首版不提供自动更新；用户手动运行新版安装包覆盖升级，保留全部用户数据和作品文件。
- 2026-08-06：不提供开机自启动；关闭窗口时进入系统托盘并继续提供手机服务，通过托盘菜单才能彻底退出。
- 2026-08-06：任务进行中退出时，可选择等待完成后退出，或安全取消当前任务后退出；关闭到托盘不影响任务。
- 2026-08-06：支持 64 位 Windows 10 和 Windows 11，不支持 Windows 7/8。
- 2026-08-07：默认封面不再识别固定的 `001.webp` 或数字页码；ZIP 内可读图片按文件名自然升序排列，第一张作为默认封面。
- 2026-08-07：详情预览不再匹配固定文件名；改为取自然排序结果中的第 4、7、10、13、16 张，最多五张。
- 2026-08-07：阅读器默认由“适配宽度”改为“适配大小”，图片同时受阅读区域宽度和高度限制并完整显示；工具栏按钮同步改名。
- 2026-08-07：Windows 作品列表改为双击进入详情，避免单击误触；手机仍单击进入。阅读器打开时隐藏详情，退出阅读器后恢复原详情页。
- 2026-08-07：阅读进度改为仅在正常退出阅读器时保存一次；阅读期间的翻页、滚动、跳页和模式切换只更新临时位置，异常退出不保存本次进度。
- 2026-08-07：阅读器移除全屏按钮；Windows 使用自定义顶部标题栏和底部工具栏，两栏在鼠标离开约 5 秒后一起滑出，靠近顶部或底部时一起滑回；窗口保留可见边框和标题栏拖动；首次阅读默认纵向连续模式。
