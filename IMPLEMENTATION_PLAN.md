# H库开发实施计划

## 1. 开发原则

- 每个阶段必须有可运行成果和验收条件。
- 先完成本地核心，再加入局域网手机端。
- 文件覆盖、目录迁移、恢复等高风险功能最后实现，并先写故障测试。
- Windows UI、手机 UI 不复制业务规则，统一调用业务服务。
- Ubuntu 上运行逻辑和 UI 测试；Windows CI 每阶段持续构建；关键里程碑在真实 Windows 验收。

## 2. 阶段 0：工程骨架

### 目标

建立可持续开发、测试和 Windows 打包的基础。

### 内容

- Python 项目结构、依赖锁定和代码质量工具。
- React/Vite 手机项目结构。
- PySide6 空窗口、托盘图标和 FastAPI 健康检查。
- SQLite、SQLAlchemy、Alembic 初始配置。
- pytest、前端测试和 GitHub Actions Windows 流程。
- PyInstaller 与 Inno Setup 最小安装包。

### 验收

- Ubuntu 可启动桌面开发窗口和 API。
- Windows CI 可生成并上传安装包。
- Windows 10/11 可安装、打开、关闭到托盘、彻底退出。

## 3. 阶段 1：数据库与文件扫描

### 内容

- 首次启动向导和主目录设置。
- 创建/识别“插画”“备份”目录。
- ZIP 漫画和图片插画校验。
- 作品、Tag、分组、关联、设置数据模型。
- 启动扫描、手动扫描、目录实时监控。
- 新增、缺失、重命名、内容替换识别。
- 异常文件去重记录。
- 通知中心基础数据。

### 验收

- 对测试目录的新增/删除/改名/替换结果符合需求。
- 重启后数据库资料稳定。
- 中文、日文、emoji 和前导零文件名正确。
- 半写入 ZIP 不会被提前收录。

## 4. 阶段 2：Windows 作品管理 MVP

### 内容

- 作品首页、搜索、排序、50 条分页。
- 左侧 Tag 筛选栏、ANY/ALL、星级筛选。
- Tag 搜索和 Tag 管理悬空卡片。
- 普通 Tag 隐式分组、同名条件前缀。
- 单列作品条目、封面缩略图和缓存。
- 漫画/插画详情页。
- 标题、Tag、封面、星级编辑。
- 封面选择器。

### 验收

- 146 部当前规模使用流畅。
- 生成 10,000 条模拟元数据时搜索、筛选和分页可用。
- 返回列表恢复搜索、筛选、页码和滚动位置。
- 所有资料修改只能从详情编辑进入。

## 5. 阶段 3：Windows 漫画阅读器

### 内容

- ZIP 图片按文件名自然排序并按需解码；第一张作为默认封面，第 4、7、10、13、16 张作为详情预览。
- 连续模式与单页模式。
- 缩放、拖动、适配和跳页；顶部标题栏与底部工具栏独立自动隐藏，不提供全屏按钮。
- 键盘方向键。
- 阅读模式设备偏好。
- 共享阅读进度和 5 秒恢复按钮。
- 详情五图预览与按住临时大图。

### 验收

- 大漫画不会一次解码全部页面到内存。
- 两种模式仅在正常退出阅读器时保存一次最终页码；连续模式同时保存页内相对位置。
- 退出重进默认开头，可在 5 秒内恢复。
- 损坏成员有明确错误，不导致整个程序崩溃。

## 6. 阶段 4：上传与批量编辑

### 内容

- Windows 按钮选择、拖放和上传悬空卡片。
- 临时区、校验、公共 Tag、逐项资料。
- 同名整批覆盖/取消。
- 最终确认和事务式提交/回滚。
- 上传任务进度、取消、退出等待。
- 外部新增通知进入批量编辑。

### 验收

- 取消不会留下正式文件或脏数据。
- 同名覆盖在最终确认前不修改旧作品。
- 人工注入提交失败时完整回滚。
- 批量公共 Tag 可被单项移除。

## 7. 阶段 5：手机配对与远程管理

### 内容

- 局域网 API、二维码和配对码。
- 设备凭证、撤销、设备列表、速率限制。
- 手机作品页、筛选抽屉、详情、编辑、通知和设置。
- 手机批量上传。
- SSE 实时通知/任务状态。
- 多电脑连接列表。
- 后端手机权限限制。

### 验收

- iPhone Safari 与 Android Chrome 同一 Wi-Fi 可配对。
- 第二次打开无需扫码。
- 多手机同时访问不会锁库或覆盖错误。
- 撤销后凭证立即失效。
- 手机无法调用目录迁移、备份恢复或退出服务接口。

## 8. 阶段 6：手机阅读器与会话离线缓存

### 内容

- 手机连续/单页模式和触摸区域。
- 工具栏 5 秒隐藏。
- 整部漫画当前会话临时缓存。
- 空间不足清理一次、重试、再降级在线。
- 离线进度暂存与重连同步。
- 内容指纹和进度冲突规则。

### 验收

- 下载部分页面后可边读边缓存。
- 断开 Wi-Fi 可阅读已缓存整部漫画。
- 退出阅读器清理图片缓存。
- 浏览器异常关闭后的残留下次清理。
- 离线/在线冲突取更后进度，不套用到已替换内容。

## 9. 阶段 7：备份、迁移和缓存治理

### 内容

- 每日首次启动和凌晨 2 点自动备份。
- 自动 5 份、手动不限、恢复前保护备份。
- 恢复确认、校验和扫描校正。
- 1GB/2GB/5GB/不限缓存和 LRU 清理。
- 主目录迁移预检、同盘/跨盘事务、取消和回滚。

### 验收

- 备份恢复不会修改媒体原文件。
- 新备份失败不会删除旧自动备份。
- 跨盘迁移中断后至少一侧保持完整可用。
- 冲突时迁移零变更。
- 旧目录只在真正为空时删除。

## 10. 阶段 8：安装、兼容和发布验收

### 内容

- 标准安装、覆盖升级、卸载数据选择。
- Windows 防火墙局域网访问引导。
- 浅色/深色/跟随系统视觉完善。
- 占位图标替换接口。
- 性能、内存、异常恢复、安全和兼容测试。
- 用户操作说明。

### 验收

- Windows 10/11 安装、升级、卸载正常。
- 覆盖升级不丢数据。
- 真实 iPhone/Android 完整流程通过。
- 主要任务失败均有可理解提示和恢复路径。

## 11. 测试结构

### 单元测试

- 文件分类、编号提取、Unicode 规范化和自然排序。
- ANY/ALL 查询、星级筛选、分页稳定性。
- Tag 删除/分组删除影响规则。
- 进度比较和离线冲突。

### 集成测试

- 临时目录扫描、watchdog 事件合并。
- SQLite 事务和 Alembic 迁移。
- ZIP 解码、缓存失效和 LRU。
- 上传、覆盖、迁移和恢复回滚。
- API 配对、撤销和权限。

### UI 测试

- PySide6 关键页面状态。
- 手机响应式布局和触摸交互。
- 悬空卡片未保存确认。
- 阅读器翻页、缩放、返回状态。

### Windows 验收

- 中文路径、长路径、不同盘符、休眠唤醒、托盘红点和防火墙。

## 12. 建议的首个可用版本边界

第一个真正可日常使用的本地版本建议完成阶段 0～4：

- Windows 安装包。
- 扫描、搜索、Tag、评分、详情和阅读器。
- Windows 本地上传。

手机功能从阶段 5 开始接入。这样可以先在真实 Windows 上验证文件与阅读核心，再扩展远程端。
