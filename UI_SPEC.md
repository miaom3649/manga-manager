# H库页面结构与文字线框

## 1. 信息架构

全局按钮规范：普通按钮使用透明背景和主题色高亮边框；仅“保存”和“开始阅读”作为主要动作按钮使用主题色实心填充及白色文字。Tag 按钮继续使用各自的选中、未选中及分类颜色规则。

全局表单控件规范：搜索框、文本输入框、下拉选择框、数字/页码框使用主题色高亮圆角边框，获得焦点时进一步强化；模式单选框的指示器使用主题色边框，选中时使用主题色填充。

所有下拉框的弹出菜单也使用应用主题，不采用操作系统默认菜单风格：菜单使用主题背景和文字颜色、灰玫瑰边框及选中背景、白色选中文字和统一 34 px 最小行高，不显示系统默认橙色高亮。下拉框右侧保留清晰可见的原生箭头，不单独覆盖箭头子控件。

应用只保留三个主页面：

```text
作品
通知
设置
```

非主导航界面：

- 作品详情页。
- 漫画阅读器。
- Tag 管理悬空卡片。
- 上传悬空卡片。
- 封面选择悬空卡片。
- 新增/缺失/替换等通知详情。
- 首次启动向导。

Windows 主页面不使用左侧导航。顶部主题栏左侧显示应用图标和“H库”名称，点击可返回作品页；右侧使用两个 44 × 44 px 的正方形高亮图标按钮进入通知和设置，通知按钮保留未读红色数字角标。手机仍使用底部导航。详情和阅读器使用返回路径，不占主导航入口。

## 2. Windows 作品首页

```text
┌─────────────────────────────────────────────────────────┐
│ [图标 H库]                              [通知③] [设置] │
├─────────────────────────────────────────────────────────┤
│ [搜索编号或标题_______________________________________] │
│ [最近添加▼] [降/升序] [上传] [刷新]                    │
├────────────┬────────────────────────────────────────────┤
│ Tag 筛选   │  共 146 条          第 1 / 3 页            │
│ [搜索Tag__]│ ┌────────────────────────────────────────┐ │
│ 漫画 插画  │ │ 封面  标题                            │ │
│ 星级筛选   │ │       459808                          │ │
│ 任意/全部  │ │       [Tag][Tag][Tag][+N]             │ │
│ 普通Tag... │ │       ★★★                             │ │
│            │ └────────────────────────────────────────┘ │
│ [管理]     │ ...固定高度单列，共 50 条...               │
│            │ [首页] [上一页] 1 2 3 [下一页] [末页]      │
└────────────┴────────────────────────────────────────────┘
```

### 顶部区域

- 主题栏使用低饱和砖红底色 `#704846`，底部显示 3 px 浅砖红强调线 `#a86f68`；左侧显示应用图标和“H库”，右侧显示通知、设置两个正方形图标按钮。手机顶部主题栏使用相同色板。
- 作品首页不显示“作品”标题，也不在顶部显示当前漫画目录路径。
- 搜索框在主题栏下方独占一整行并固定为 48 px 高；下一行左对齐依次放置排序、升降序、上传和刷新控件，四个控件统一为 40 px 高，不显示“选择漫画目录”或“重新扫描”按钮。
- 搜索框只匹配真实文件名/编号和标题。
- 英文逗号分隔批量关键词。
- 停止输入 0.4 秒自动搜索；Enter 和搜索按钮立即搜索。
- 排序菜单：添加时间、文件名/编号、标题、星级。
- 主页面排序下拉框和星级筛选下拉框的当前值及展开菜单项均居中显示。
- 居中下拉框必须保持原生非编辑式下拉交互，不得通过只读输入框模拟，以免输入框内边距导致文字偏移或拦截点击。
- 独立方向按钮切换升序/降序。
- 上传按钮打开上传悬空卡片。

### 左侧筛选栏

- 筛选卡片顶部不显示“Tag 筛选”标题，进入卡片后直接显示匹配模式与筛选控件。
- 常驻，不按分组分层。
- Tag 使用自适应两列布局：短名称一行显示两个，超过半栏宽度的名称自动独占整行；整行仍放不下时显示省略号，并通过鼠标悬停提示完整名称。筛选区不显示横向滚动条。
- 自定义 Tag 在筛选栏、作品栏、详情页和编辑页内统一按布局类型稳定分段排列，但不显示分段标题：带 `分组：` 前缀的 Tag 一律归为长 Tag并排在最上方；其后是其他无前缀长 Tag；所有短 Tag 排在最下方。同一类型内部保持原有自然顺序。筛选栏中的长 Tag 继续独占整行、短 Tag 使用两列。
- 编辑页 Tag 区使用响应式单位网格，不固定为 6 列：短 Tag 固定占 1 单位，长 Tag 固定占 3 单位，每行单位数根据 Tag 区实际宽度动态计算，并在窗口缩放时重新排列。Tag 按统一顺序连续填充，长 Tag 后的所有实际剩余单位都可继续放短 Tag；单位宽 48 px、间距 7 px，长 Tag 总宽 158 px。
- 详情页 Tag 区使用与编辑页相同的响应式单位网格和宽度规格；系统 Tag 固定排在最前，后续长短 Tag 按统一分类顺序连续填充，详情卡片宽度变化时自动重排。每个网格单位严格保持 48 px 并整体左对齐，Tag 较少时剩余空间只能留在整组右侧，不得被平均分配到 Tag 之间。
- 详情页所有 Tag 胶囊均可点击。点击漫画/插画系统 Tag 时关闭详情并回到作品主页，只保留对应类型筛选；点击作者或其他自定义 Tag 时关闭详情并回到主页，清除原搜索、星级和其他 Tag 条件，只保留当前作品的漫画/插画系统 Tag与刚点击的自定义 Tag，并将普通 Tag 匹配模式设为“全部匹配”。排序方式保持不变。
- Tag 名称和 Tag 分组名称分别限制为最多 5 个字符；Windows 创建与重命名输入框直接限制长度，数据服务同时执行统一校验。
- 系统自动提供不可改名、不可删除的“作者”系统分组。作者 Tag 不受 5 字限制，不显示 `作者：` 前缀，使用黄色胶囊并强制按长 Tag 布局；在显示系统类型 Tag 的位置，作者 Tag 紧跟漫画/插画之后，再排列其他前缀长 Tag、普通长 Tag和短 Tag。
- 同名不同组的 Tag 需要显示 `分组：Tag` 时，主页面筛选按钮、作品栏 Tag 胶囊和详情页 Tag 胶囊都将 `分组：` 前缀绘制为暗色背景下清晰可见的柔和灰粉色，实际 Tag 名称保持普通文字颜色；整段能够放下时不得单独截断前缀。
- 系统 Tag“漫画/插画”固定在前，使用系统强调色。
- 已分组普通 Tag 使用主题色；未分组使用灰色。
- 搜索 Tag 同时匹配 Tag 名和隐藏分组名。
- 普通 Tag ANY/ALL 切换只影响普通 Tag。
- 星级支持指定星级、至少某星级、未评价。
- 任一条件改变后立即刷新并回第 1 页。
- “管理”按钮打开 Tag 管理卡片。

### 作品条目

- 单击作品条目时只显示主题色高亮边框；点击主软件窗口内任何未被作品或交互控件占用的空白背景区域时，清除当前选择并立即移除高亮边框。按钮、输入框、Tag、通知及作品本身不视为空白区域。

```text
┌────────────────────────────────────────────────────────┐
│  [无框封面]  用户标题                                   │
│   圆角阴影    459808                                    │
│               [Tag][Tag][Tag][+N]                       │
│               ★★★                                      │
└────────────────────────────────────────────────────────┘
```

- 每条固定高度。
- 封面完整显示，不裁剪；不可见布局区内尽量贴边，图片轮廓有轻微圆角和阴影。
- 漫画显示纯编号；非数字 ZIP 编号行为空；插画显示原文件名。
- Tag 单行从左向右展示，不可点击，也不提供横向滚动。空间不足时隐藏后续 Tag，并在末尾用灰色 `+N` 胶囊显示被隐藏的数量。
- Tag 摘要区域不创建滚动视口、轨道或滑块，并且不拦截鼠标；点击该区域等同于点击作品条目本身。
- 星级只展示。
- Windows 双击条目非滚动控件区域进入详情；手机单击进入详情。
- Windows 单击选择条目时只显示包围整条卡片的主题色高亮边框，不用系统选择色填充条目背景。

## 3. 手机作品首页

```text
┌─────────────────────────┐
│ H库       通知③          │
│ [搜索编号或标题________]  │
│ [筛选] [排序▼] [⇅] [上传] │
│ ┌─────────────────────┐ │
│ │ 封面  标题           │ │
│ │       459808         │ │
│ │       Tag →          │ │
│ │       ★★★            │ │
│ └─────────────────────┘ │
│ ...50 条...             │
│ [首页] [上页] 1/3 [下页] │
├─────────────────────────┤
│ 作品     通知     设置   │
└─────────────────────────┘
```

- “筛选”打开侧边抽屉；一次选择后抽屉保持打开。
- 手机条目仍为单列，按屏幕压缩封面和间距。
- 页码控件保留首页/上一页/下一页/末页与跳页入口，可在窄屏折叠次要按钮。
- 重新打开网页时清空搜索和筛选，只保留设备偏好。

## 4. 筛选抽屉（手机）

```text
┌─────────────────────┐
│ 筛选            [×] │
│ [任意匹配][全部匹配]│
│ [搜索 Tag_________] │
│ [漫画][插画][Tag]...│
│ 星级  [未评][★][★★] │
│                     │
│ [管理] [清除全部]   │
└─────────────────────┘
```

Tag 管理按钮位于 Tag 筛选卡片内部，打开居中悬空管理卡片，而不是跳转页面。Tag 搜索框同时过滤系统 Tag 与自定义 Tag；例如输入“漫画”时只显示漫画系统 Tag，不显示插画系统 Tag。

系统 Tag 与自定义 Tag 不使用标题、分隔栏或独立边框区分，全部连续排列在同一按钮区域。按钮使用一致的胶囊形式：选中时只通过填充颜色表达状态，不添加对勾字符；未选中时仅显示彩色边框。匹配模式位于所有 Tag 上方。

## 5. 漫画详情页

Windows 详情页使用主窗口内部的通用浮空卡片模板，不再作为屏幕居中的独立窗口；手机继续使用对应的网页详情界面。

详情页的系统 Tag 和自定义 Tag 不使用纯文本拼接，而显示为比筛选栏按钮更紧凑的小号圆角胶囊，并自动排列为多行。系统 Tag 使用系统强调色，已分组 Tag 使用主题色，未分组 Tag 使用灰色。

详情页顶部不显示“漫画详情”或“插画详情”标题。编辑按钮位于内容底部；漫画详情中放在“开始阅读”正下方并与其等宽，插画详情中同样作为底部整行按钮。

“开始阅读”是整行主操作按钮，使用主题色高亮边框和加粗文字。

详情编辑状态中的 Tag 也使用胶囊按钮：选中时填充颜色但不添加对勾字符，未选中时只显示圆角边框；已分组 Tag 使用主题色，未分组 Tag 使用灰色。

详情编辑状态是详情页的下一级：点击浮空卡片外遮罩或按 Esc 时返回详情，而不是关闭详情回到作品列表。只有标题、星级、Tag 或封面确实发生变化时才询问是否放弃；没有变化直接返回详情。编辑页底部不显示取消按钮，保存按钮位于更改封面按钮下方、与其等宽并使用高亮边框；保存后返回详情。

### Tag 胶囊尺寸规范

- 详情页、主页作品栏和详情编辑页统一采用详情页胶囊规格：高度固定为 `26 px`，圆角 `9 px`，水平内边距 `8 px`；宽度根据 Tag 名称自然计算，不固定为统一宽度。
- 详情编辑页的胶囊紧贴搜索框下方，从区域左上角开始按 Tag 顺序由左向右连续追加，空间不足时换到下一行，不允许整组居中。
- 详情编辑页不显示搜索框上方的“Tag”标题。Tag 搜索框紧接星级字段下方，其后的可滚动 Tag 区域至少高 `250 px`，用于排列固定尺寸胶囊。
- 主筛选栏的可点击筛选胶囊可按筛选用途使用既有较大尺寸。

```text
┌──────────────────────────────────────────┐
│        无框圆角悬浮封面                   │
│                                          │
│ 标题                                     │
│ 459808                                   │
│ ★★★                                      │
│ [Tag][Tag][Tag] 自动换行                  │
│                                          │
│ ╭──────────────────────────────────────╮ │
│ │ 第4  第7  第10  第13  第16张 → 滑动 │ │
│ ╰──────────────────────────────────────╯ │
│                                          │
│              [开始阅读]                  │
│                [编辑]                    │
└──────────────────────────────────────────┘
```

- 顶部封面为当前封面，默认使用 ZIP 内图片自然排序后的第一张。
- 编号只读；非数字 ZIP 留空。
- Tag 完整换行，只读。
- 预览容器有圆角边框和阴影，内部图片统一高度、宽度按比例变化。
- 预览内容取自然排序后的第 4、7、10、13、16 张，作品较短时有多少显示多少。
- Windows 按住鼠标立即临时大图；手机保持不动 0.5 秒临时大图，松开关闭。
- 点击封面或预览不进入阅读器。
- 开始阅读按钮进入阅读器。

### 编辑状态

- 可编辑标题、星级、普通 Tag 和封面。
- 编号/真实文件名、类型 Tag 不可编辑。
- Tag 搜索匹配名称和隐藏分组。
- “管理 Tag”可叠加打开 Tag 管理卡片；新建 Tag 自动加入当前作品待保存选择。
- 保存统一提交作品资料；取消不写入作品关联，但不会删除已经创建的 Tag。

## 6. 插画详情页

```text
┌──────────────────────────────────────────┐
│ [返回]                         [编辑]     │
│                                          │
│        无框圆角悬浮原图                   │
│                                          │
│ 标题                                     │
│ original-file.gif                        │
│ ★★                                       │
│ [Tag][Tag][Tag] 自动换行                  │
└──────────────────────────────────────────┘
```

- 不显示漫画五图预览栏。
- 不进入漫画阅读器。
- 不提供缩放、拖动和翻页工具。
- GIF 保持动画显示。

## 7. 漫画阅读器

### Windows

```text
┌────────────────────────────────────────────────────────┐
│ ←                       作品标题                        │
├────────────────────────────────────────────────────────┤
│                                                        │
│                    漫画页面                            │
│                                                        │
├────────────────────────────────────────────────────────┤
│ 35/180 | 连续/单页 | − 100% + | 适配大小 | 跳页        │
└────────────────────────────────────────────────────────┘
```

- 刚打开时顶部标题栏和底部工具栏均显示。
- Windows 标题栏设计高度为 68 px，底部工具栏设计高度为 72 px；按钮和输入控件至少高 40 px，模式选择、适配大小和页码框预留足够宽度，文字不得裁切。
- 阅读器不沿用主界面的灰粉高亮按钮风格。顶部与底部的容器本身完全透明，返回、标题、页码、模式、缩放、适配和跳页分别显示为独立的深色半透明圆角小块。
- 可点击小块不显示灰粉边框；鼠标悬停或获得输入焦点时，小块背景加深但仍保持半透明。标题块和页码块为只读显示块。
- 阅读模式使用普通半透明按钮，不显示下拉箭头或弹出菜单；每次点击直接在“纵向连续”和“单页阅读”之间切换，按钮文字显示当前模式。
- 上下区域作为覆盖在漫画画面上的真正悬浮层，使各小块之间的空隙直接透出后方漫画。隐藏时通过整体位置滑出窗口，不再压缩栏高度。
- 顶部标题栏左侧只有返回箭头，标题严格居中；底部所有工具块作为一个整体水平居中，不提供全屏或前后翻页按钮。
- 鼠标离开顶部和底部约 5 秒后，标题栏向上、工具栏向下同时滑出；鼠标靠近顶部或底部任一侧时，两栏一起滑回。
- 页码框中仍有输入光标时禁止隐藏；页码框失焦且鼠标远离后重新计时 5 秒。
- 阅读器保留可见窗口边框；标题栏支持系统级拖动窗口，双击最大化或恢复。
- 保留 Esc 和 Alt+F4 快捷关闭方式。
- `←`/`↑` 上一页，`→`/`↓` 下一页（单页模式）。
- 单页模式鼠标滚轮向上翻到上一页、向下翻到下一页；工具栏不设置上一页/下一页按钮。
- 进入时适配阅读区域大小，完整显示图片且不裁剪，不继承缩放。
- 连续模式自由滚动，不吸附。
- 单页模式一次一页。
- 连续滚动时实时检测视口中央页，并同步顶部页码与跳页框；两种模式切换时从同一当前页继续。
- 跳页框不显示上下调节箭头，只保留页码输入。
- 从详情浮空卡片进入阅读器时隐藏整个软件主窗口，桌面只显示阅读器；关闭阅读器或点击“返回详情”后恢复主窗口，并在其上重新显示对应详情浮空卡片。
- “回到上次观看位置”按钮完整显示文字，并始终跳到进入阅读器时冻结的历史位置，不受按钮显示期间新浏览位置影响。
- 阅读期间仅维护临时当前位置；通过返回详情、Esc 或 Alt+F4 正常退出时才提交一次阅读进度。

### 手机

```text
┌─────────────────────────┐
│        漫画页面          │
│                         │
│ 左侧点击       右侧点击  │
│ 上一页         下一页    │
│                         │
│      下侧：工具栏        │
└─────────────────────────┘
```

- 左侧上一页，右侧下一页，下侧显示/隐藏工具栏，上侧不翻页。
- 底部触发区优先，不能同时翻页。
- 工具栏无操作 5 秒隐藏，任意交互重新计时。
- 双指缩放和拖动优先于点击。
- 有历史进度时显示 5 秒“回到上次观看位置”。
- 打开后后台缓存整部漫画；退出阅读器清理图片缓存。
- 网络断开仍可读已缓存页面，离线进度重连后同步。

### 手机网页统一主题

- 手机网页与 Windows 主程序共用低饱和黑粉视觉语言：深黑粉背景 `#171316`、内容面 `#211b1f`、灰玫瑰高亮 `#9a6f7b` 和柔和强调色 `#b58b96`。
- 顶部主题栏显示 H库 标识；通知与设置使用正方形灰玫瑰描边图标按钮，未读通知显示红色数字角标。
- 输入框、下拉框和普通按钮使用灰玫瑰高亮边框；主要操作使用灰玫瑰填色，危险操作使用红色。
- 漫画/插画系统 Tag 使用低饱和灰青色，作者分组 Tag 使用低饱和金棕色，其他已分组 Tag 使用灰玫瑰色，未分组 Tag 使用灰色。
- 首页作品使用深色悬浮卡片和无框阴影封面；标题、编号、Tag 和个人星级与 Windows 卡片保持相同信息层级。
- 详情、编辑、筛选、Tag 管理、通知、设置、确认和错误提示统一使用加深遮罩上的圆角悬浮卡片，不使用浏览器原生 `alert`、`confirm` 或 `prompt`。
- 通知卡片采用单个大区域分行显示；已读行为深色，未读行为透明，空白时保留分隔行占位。
- 手机窄屏详情卡片可以铺满屏幕，但仍保持相同的圆角、边框、颜色和控件规范。
- 阅读器继续使用黑色背景；标题栏和工具栏拆分为半透明小块，交互块按下时加深。

## 8. Tag 管理悬空卡片

```text
          半透明遮罩
   ╭──────────────────────╮
   │                      │
   │ [管理 Tag 分组] [管理 Tag] │
   │                      │
   ╰──────────────────────╯
```

- 初始选择卡片是特殊卡片：相对于当前软件窗口居中，遮罩也只覆盖当前软件窗口，不遵循其他弹窗的屏幕居中规则。
- 卡片内部只有左右并排的两个大型方形圆角按钮，每个选项都有独立、清晰的完整方框边界；不显示取消按钮，点击卡片外的半透明遮罩返回原页面。
- 选择后分别打开“管理 Tag 分组”或“管理 Tag”卡片，两类内容不再混在同一张卡片中。
- 随后打开的 Tag 分组管理和 Tag 管理页面也使用当前软件窗口内部的浮空卡片，卡片始终严格居中且不能超出窗口；沿用明显加深的半透明暗色遮罩和投影。卡片底部不显示 Close/关闭按钮，点击卡片外遮罩或按 Esc 可关闭。
- 上述窗口内遮罩与浮空卡片作为公共 UI 模板实现；后续明确指定使用“浮空卡片”的特殊页面复用该模板，只替换卡片尺寸和内部内容。
- 浮空卡片打开期间持续监听所属软件窗口尺寸：主窗口缩放时遮罩立即覆盖新的完整内容区，卡片实时重新居中；可用空间小于设计尺寸时卡片同步缩小，窗口重新放大后恢复到设计尺寸。
- Windows 和手机都使用居中悬空卡片，手机保留屏幕边距。
- 操作逐项立即生效，不显示额外的逐行保存按钮或整卡保存按钮。
- Tag 卡片顶部只有“新 Tag 名称”和“创建新 Tag”，不提供创建时选择分组；新 Tag 默认未分组，创建后可在列表中调整。分组卡片中单独创建新分组。
- 分组不在筛选栏显式展示，但在管理卡片中可见。
- “作者”系统分组的黄色 Tag 在 Windows 左侧筛选栏默认隐藏；搜索框输入“作者”时显示全部。详情页点击作者 Tag 快速筛选后，只临时显示当前选中的黄色 Tag；在左侧取消选中或清除筛选后立即隐藏。
- 删除 Tag 显示受影响作品数。
- 删除非空分组时选择将 Tag 移到未分组，或确认后删除组和 Tag。
- 点击遮罩/关闭/Esc 尝试关闭；当前输入未提交时提示是否放弃。

## 9. 上传检查与提示卡片

```text
选择文件 → 上传临时区 → 校验 → 直接加入 / 冲突确认 / 非法拒绝
```

### 步骤 1：选择与传输

- Windows 按钮选择或拖放；手机使用文件选择器。
- 显示单文件与总进度，允许取消。

### 步骤 2：校验和冲突

- 显示漫画、插画、异常和同名数量。
- 任意不合格文件会触发一张拒绝悬浮卡片，列出文件及原因，整批不加入。
- 合法且无同名项时不弹卡片，直接使用默认资料加入。
- 有同名项时只弹一次悬浮“覆盖/取消”卡片并列出冲突；覆盖立即事务式提交，取消结束整批。
- 上传入口不提供标题、Tag、星级或封面编辑；这些资料只能在加入后从作品详情页编辑。

## 10. 封面选择卡片

- 每页 50 张缩略图网格。
- 每张显示 ZIP 内文件名。
- 首页/上一页/下一页/末页/跳页。
- 当前封面有明确选中状态。
- 点击只改变待选项；确认才保存，取消保持原封面。

## 11. 通知页

```text
┌───────────────────────────────────┐
│              通知                 │
│ [删除所选通知] [清空通知]         │
│ ┌───────────────────────────────┐ │
│ │ 新增 12 个文件 · 时间         │ │
│ ├───────────────────────────────┤ │
│ │ 已读通知使用深色背景          │ │
│ ├───────────────────────────────┤ │
│ │                               │ │
│ └───────────────────────────────┘ │
└───────────────────────────────────┘
```

- 通知内容区在保留页面边距的前提下随窗口横向铺满，标题居中；两个操作按钮完整占据同一行，各占一半宽度。
- 通知列表使用一个完整的圆角外边框，内部各行紧密排列并用横线分隔，呈现类似邮件收件箱的列表；不把每条通知画成独立圆角按钮。未读通知背景透明，已读通知使用深色背景。
- 打开具体通知时才将该通知标为已读，进入通知页本身不批量改变已读状态。
- 没有通知时不显示“暂无通知”，完整通知区域保持显示；无文字行的数量和高度根据通知区域实时计算，窗口缩放后仍由横向分割线铺满到底部，不留下未分割的大块空白。
- 点击新增通知进入批量编辑列表。
- 缺失通知显示已自动清理的文件列表。
- 重命名通知显示旧名 → 新名。
- 替换通知提供“保留资料/当作新作品”。
- 通知保留 30 天，可单删或全清。
- Windows、手机共享列表和已读状态。

## 12. Windows 设置页

页面使用横向铺满可用窗口的单列内容区，所有标题和文字保持居中；操作按钮统一为相同宽度和高度并铺满内容区，目录、缓存及主题控件也与按钮列对齐。缓存容量和外观主题下拉框的当前值及展开菜单项均居中显示。页面只保留正常外边距，不设置固定最大宽度。

设置页最底部显示红色实心“恢复所有设置”按钮。点击后打开使用通用遮罩模板的浮空警告卡片，明确说明操作不可恢复。确认后暂时停止扫描和手机服务，保留当前作品目录路径并安全重建整个软件数据库，再重新扫描原文件；全部管理资料、通知、配对和设置恢复默认，同时删除备份与缓存。仅漫画、插画原文件和作品目录位置保留。重建失败时使用临时数据库副本回滚。

恢复设置警告卡片底部的“取消”和红色“确认恢复所有设置”按钮各占一半宽度，并统一固定为 46 px 高。

分组建议：

- 作品目录：当前路径、重新扫描、更换并迁移目录。
- 目录迁移只禁止选择当前目录本身，允许选择其子目录；选择子目录时，成功后保留包含新目录的外层旧目录。
- 手机连接：配对二维码、配对码、已配对设备和撤销。
- 备份恢复：自动备份状态、手动备份、恢复。
- 缓存：占用、1GB/2GB/5GB/不限、清理缓存。
- 外观：浅色/深色/跟随系统。
- 关于：H库版本、占位图标、许可证信息。

高影响操作必须有明确确认和进度界面。

## 13. 手机设置页

- 主题模式。
- 当前 Windows 电脑。
- 切换已配对电脑。
- 当前设备名称。
- 断开当前电脑。

不显示目录迁移、恢复备份、其他设备管理和退出 Windows 服务。

## 14. 首次启动向导

```text
欢迎使用 H库
→ 选择/新建主目录
→ 创建或确认“插画”“备份”目录
→ 首次扫描进度
→ 漫画/插画/异常数量摘要
→ 进入作品首页
```

手机配对留在设置页，不塞入首次向导。

## 15. 通用悬空卡片规则

- 居中、圆角、阴影、半透明遮罩。
- Windows 阅读器、选择器、配对和确认弹窗仍相对当前显示器可用区域居中；详情页及明确指定的特殊管理卡片使用主窗口内部的浮空卡片模板。
- 上述窗口在系统层面独立定位，拖动任一窗口不得带动主窗口或其他卡片一起移动。
- Windows 主窗口不强制居中；启动时恢复上次关闭时的位置、大小和最大化状态，显示器变化导致位置失效时移回可见屏幕。
- Windows 和手机都不强制跳转独立页面。
- 内容超高时卡片内部滚动。
- 关闭后保留底层页面搜索、筛选、排序、页码和滚动位置。
- 有未提交输入时，点击遮罩、关闭或 Esc 弹出放弃确认。
- 已经逐项即时保存的 Tag 管理操作不会因关闭卡片而撤销。

## 16. 视觉基线

- 首版固定主色；系统 Tag 使用独立强调色，未分组 Tag 使用灰色。
- 支持浅色、深色、跟随系统。
- 图片无框悬浮、轻微圆角、柔和黑色阴影。
- 详情预览栏是有边框的悬浮容器，与无框封面形成层级差异。
- 正文和控件只使用简体中文；用户内容完整支持 Unicode。
- 动画和过渡保持短促，不阻塞操作，也尊重系统“减少动态效果”设置。
