# H库页面结构与文字线框

## 1. 信息架构

主导航只保留三个入口：

```text
作品
通知
设置
```

非主导航界面：

- 作品详情页。
- 漫画阅读器。
- Tag 管理悬空卡片。
- 上传悬空卡片。
- 封面选择悬空卡片。
- 新增/缺失/替换等通知详情。
- 首次启动向导。

Windows 使用左侧窄导航；手机使用底部导航。详情和阅读器使用返回路径，不占主导航入口。

## 2. Windows 作品首页

```text
┌──────┬──────────────────────────────────────────────────┐
│ H库  │  [搜索编号或标题________________] [排序▼] [⇅] [上传] │
│      ├────────────┬─────────────────────────────────────┤
│ 作品 │ Tag 筛选    │  共 146 条          第 1 / 3 页       │
│ 通知③│ [搜索Tag__] │ ┌─────────────────────────────────┐ │
│ 设置 │ 漫画 插画   │ │ 封面  标题                       │ │
│      │ 星级筛选    │ │       459808                     │ │
│      │ 任意/全部   │ │       Tag Tag Tag →              │ │
│      │             │ │       ★★★                        │ │
│      │ 普通Tag...  │ └─────────────────────────────────┘ │
│      │             │ ...固定高度单列，共 50 条...        │
│      │ [管理]      │ [首页] [上一页] 1 2 3 [下一页] [末页] │
└──────┴────────────┴─────────────────────────────────────┘
```

### 顶部区域

- 搜索框只匹配真实文件名/编号和标题。
- 英文逗号分隔批量关键词。
- 停止输入 0.4 秒自动搜索；Enter 和搜索按钮立即搜索。
- 排序菜单：添加时间、文件名/编号、标题、星级。
- 独立方向按钮切换升序/降序。
- 上传按钮打开上传悬空卡片。

### 左侧筛选栏

- 常驻，不按分组分层。
- 系统 Tag“漫画/插画”固定在前，使用系统强调色。
- 已分组普通 Tag 使用主题色；未分组使用灰色。
- 搜索 Tag 同时匹配 Tag 名和隐藏分组名。
- 普通 Tag ANY/ALL 切换只影响普通 Tag。
- 星级支持指定星级、至少某星级、未评价。
- 任一条件改变后立即刷新并回第 1 页。
- “管理”按钮打开 Tag 管理卡片。

### 作品条目

```text
┌────────────────────────────────────────────────────────┐
│  [无框封面]  用户标题                                   │
│   圆角阴影    459808                                    │
│               [Tag][Tag][Tag][Tag] → 横向滚动           │
│               ★★★                                      │
└────────────────────────────────────────────────────────┘
```

- 每条固定高度。
- 封面完整显示，不裁剪；不可见布局区内尽量贴边，图片轮廓有轻微圆角和阴影。
- 漫画显示纯编号；非数字 ZIP 编号行为空；插画显示原文件名。
- Tag 单行横向滚动，只展示，不可点击。
- 星级只展示。
- Windows 双击条目非滚动控件区域进入详情；手机单击进入详情。

## 3. 手机作品首页

```text
┌─────────────────────────┐
│ H库       通知③          │
│ [搜索编号或标题________]  │
│ [筛选] [排序▼] [⇅] [上传] │
│ ┌─────────────────────┐ │
│ │ 封面  标题           │ │
│ │       459808         │ │
│ │       Tag →          │ │
│ │       ★★★            │ │
│ └─────────────────────┘ │
│ ...50 条...             │
│ [首页] [上页] 1/3 [下页] │
├─────────────────────────┤
│ 作品     通知     设置   │
└─────────────────────────┘
```

- “筛选”打开侧边抽屉；一次选择后抽屉保持打开。
- 手机条目仍为单列，按屏幕压缩封面和间距。
- 页码控件保留首页/上一页/下一页/末页与跳页入口，可在窄屏折叠次要按钮。
- 重新打开网页时清空搜索和筛选，只保留设备偏好。

## 4. 筛选抽屉（手机）

```text
┌─────────────────────┐
│ 筛选            [×] │
│ [搜索 Tag_________] │
│ 类型  [漫画] [插画] │
│ 星级  [未评][★][★★] │
│ 普通 Tag：任意/全部 │
│ [Tag][Tag][Tag] ... │
│                     │
│ [管理] [清除全部]   │
└─────────────────────┘
```

Tag 管理按钮打开居中悬空卡片，而不是跳转页面。

## 5. 漫画详情页

```text
┌──────────────────────────────────────────┐
│ [返回]                         [编辑]     │
│                                          │
│        无框圆角悬浮封面                   │
│                                          │
│ 标题                                     │
│ 459808                                   │
│ ★★★                                      │
│ [Tag][Tag][Tag] 自动换行                  │
│                                          │
│ ╭──────────────────────────────────────╮ │
│ │ 第4  第7  第10  第13  第16张 → 滑动 │ │
│ ╰──────────────────────────────────────╯ │
│                                          │
│              [开始阅读]                  │
└──────────────────────────────────────────┘
```

- 顶部封面为当前封面，默认使用 ZIP 内图片自然排序后的第一张。
- 编号只读；非数字 ZIP 留空。
- Tag 完整换行，只读。
- 预览容器有圆角边框和阴影，内部图片统一高度、宽度按比例变化。
- 预览内容取自然排序后的第 4、7、10、13、16 张，作品较短时有多少显示多少。
- Windows 按住鼠标立即临时大图；手机保持不动 0.5 秒临时大图，松开关闭。
- 点击封面或预览不进入阅读器。
- 开始阅读按钮进入阅读器。

### 编辑状态

- 可编辑标题、星级、普通 Tag 和封面。
- 编号/真实文件名、类型 Tag 不可编辑。
- Tag 搜索匹配名称和隐藏分组。
- “管理 Tag”可叠加打开 Tag 管理卡片；新建 Tag 自动加入当前作品待保存选择。
- 保存统一提交作品资料；取消不写入作品关联，但不会删除已经创建的 Tag。

## 6. 插画详情页

```text
┌──────────────────────────────────────────┐
│ [返回]                         [编辑]     │
│                                          │
│        无框圆角悬浮原图                   │
│                                          │
│ 标题                                     │
│ original-file.gif                        │
│ ★★                                       │
│ [Tag][Tag][Tag] 自动换行                  │
└──────────────────────────────────────────┘
```

- 不显示漫画五图预览栏。
- 不进入漫画阅读器。
- 不提供缩放、拖动和翻页工具。
- GIF 保持动画显示。

## 7. 漫画阅读器

### Windows

```text
┌────────────────────────────────────────────────────────┐
│ ←                       作品标题                        │
├────────────────────────────────────────────────────────┤
│                                                        │
│                    漫画页面                            │
│                                                        │
├────────────────────────────────────────────────────────┤
│ 35/180 | 连续/单页 | − 100% + | 适配大小 | 跳页        │
└────────────────────────────────────────────────────────┘
```

- 刚打开时顶部标题栏和底部工具栏均显示。
- 顶部标题栏左侧只有返回箭头，标题严格居中；底部工具栏不提供全屏或前后翻页按钮。
- 鼠标离开顶部和底部约 5 秒后，标题栏向上、工具栏向下同时滑出；鼠标靠近顶部或底部任一侧时，两栏一起滑回。
- 页码框中仍有输入光标时禁止隐藏；页码框失焦且鼠标远离后重新计时 5 秒。
- 阅读器保留可见窗口边框；标题栏支持系统级拖动窗口，双击最大化或恢复。
- 保留 Esc 和 Alt+F4 快捷关闭方式。
- `←`/`↑` 上一页，`→`/`↓` 下一页（单页模式）。
- 单页模式鼠标滚轮向上翻到上一页、向下翻到下一页；工具栏不设置上一页/下一页按钮。
- 进入时适配阅读区域大小，完整显示图片且不裁剪，不继承缩放。
- 连续模式自由滚动，不吸附。
- 单页模式一次一页。
- 连续滚动时实时检测视口中央页，并同步顶部页码与跳页框；两种模式切换时从同一当前页继续。
- 跳页框不显示上下调节箭头，只保留页码输入。
- 从详情页进入时关闭详情窗口；关闭阅读器或点击“返回详情”后重新打开对应详情页，避免隐藏窗口阻塞主界面。
- “回到上次观看位置”按钮完整显示文字，并始终跳到进入阅读器时冻结的历史位置，不受按钮显示期间新浏览位置影响。
- 阅读期间仅维护临时当前位置；通过返回详情、Esc 或 Alt+F4 正常退出时才提交一次阅读进度。

### 手机

```text
┌─────────────────────────┐
│        漫画页面          │
│                         │
│ 左侧点击       右侧点击  │
│ 上一页         下一页    │
│                         │
│      下侧：工具栏        │
└─────────────────────────┘
```

- 左侧上一页，右侧下一页，下侧显示/隐藏工具栏，上侧不翻页。
- 底部触发区优先，不能同时翻页。
- 工具栏无操作 5 秒隐藏，任意交互重新计时。
- 双指缩放和拖动优先于点击。
- 有历史进度时显示 5 秒“回到上次观看位置”。
- 打开后后台缓存整部漫画；退出阅读器清理图片缓存。
- 网络断开仍可读已缓存页面，离线进度重连后同步。

## 8. Tag 管理悬空卡片

```text
          半透明遮罩
   ╭──────────────────────╮
   │ Tag 管理          [×] │
   │ [搜索______________] │
   │                      │
   │ [＋ 添加 Tag]        │
   │ Tag名称  隐式分组  ⋯ │
   │ Tag名称  未分组    ⋯ │
   │                      │
   │ 分组管理              │
   ╰──────────────────────╯
```

- Windows 和手机都使用居中悬空卡片，手机保留屏幕边距。
- 操作逐项确认后立即保存。
- 创建 Tag：名称 + 已有分组/未分组/新建分组。
- 分组不在筛选栏显式展示，但在管理卡片中可见。
- 删除 Tag 显示受影响作品数。
- 删除非空分组时选择将 Tag 移到未分组，或确认后删除组和 Tag。
- 点击遮罩/关闭/Esc 尝试关闭；当前输入未提交时提示是否放弃。

## 9. 上传悬空卡片

```text
选择文件 → 上传临时区 → 校验/冲突 → 批量资料 → 单项资料 → 最终确认
```

### 步骤 1：选择与传输

- Windows 按钮选择或拖放；手机使用文件选择器。
- 显示单文件与总进度，允许取消。

### 步骤 2：校验和冲突

- 显示漫画、插画、异常和同名数量。
- 不合格项必须移除或取消任务。
- 有同名项时只弹一次“覆盖/取消”；取消结束整批，覆盖只进入下一步。

### 步骤 3：批量资料

- 公共 Tag、批量星级（可不设）。
- 公共 Tag 可在单项中移除。

### 步骤 4：单项资料

- 标题、Tag、星级。
- 漫画封面选择；插画使用自身。
- 可打开 Tag 管理卡片。

### 步骤 5：最终确认

- 文件总数、覆盖数、目标位置、空字段默认规则。
- 返回上一步不写入正式目录。
- “确定上传”才开始事务式提交。

## 10. 封面选择卡片

- 每页 50 张缩略图网格。
- 每张显示 ZIP 内文件名。
- 首页/上一页/下一页/末页/跳页。
- 当前封面有明确选中状态。
- 点击只改变待选项；确认才保存，取消保持原封面。

## 11. 通知页

```text
┌───────────────────────────────────┐
│ 通知        [全部已读] [清空全部] │
│ ● 新增 12 个文件           时间   │
│ ● 3 个文件被重命名          时间   │
│   2 个文件丢失并已清理       时间   │
│ ● 459808 内容已替换，待处理  时间   │
└───────────────────────────────────┘
```

- 点击新增通知进入批量编辑列表。
- 缺失通知显示已自动清理的文件列表。
- 重命名通知显示旧名 → 新名。
- 替换通知提供“保留资料/当作新作品”。
- 通知保留 30 天，可单删或全清。
- Windows、手机共享列表和已读状态。

## 12. Windows 设置页

分组建议：

- 作品目录：当前路径、重新扫描、更换并迁移目录。
- 手机连接：配对二维码、配对码、已配对设备和撤销。
- 备份恢复：自动备份状态、手动备份、恢复。
- 缓存：占用、1GB/2GB/5GB/不限、清理缓存。
- 外观：浅色/深色/跟随系统。
- 关于：H库版本、占位图标、许可证信息。

高影响操作必须有明确确认和进度界面。

## 13. 手机设置页

- 主题模式。
- 当前 Windows 电脑。
- 切换已配对电脑。
- 当前设备名称。
- 断开当前电脑。

不显示目录迁移、恢复备份、其他设备管理和退出 Windows 服务。

## 14. 首次启动向导

```text
欢迎使用 H库
→ 选择/新建主目录
→ 创建或确认“插画”“备份”目录
→ 首次扫描进度
→ 漫画/插画/异常数量摘要
→ 进入作品首页
```

手机配对留在设置页，不塞入首次向导。

## 15. 通用悬空卡片规则

- 居中、圆角、阴影、半透明遮罩。
- Windows 和手机都不强制跳转独立页面。
- 内容超高时卡片内部滚动。
- 关闭后保留底层页面搜索、筛选、排序、页码和滚动位置。
- 有未提交输入时，点击遮罩、关闭或 Esc 弹出放弃确认。
- 已经逐项即时保存的 Tag 管理操作不会因关闭卡片而撤销。

## 16. 视觉基线

- 首版固定主色；系统 Tag 使用独立强调色，未分组 Tag 使用灰色。
- 支持浅色、深色、跟随系统。
- 图片无框悬浮、轻微圆角、柔和黑色阴影。
- 详情预览栏是有边框的悬浮容器，与无框封面形成层级差异。
- 正文和控件只使用简体中文；用户内容完整支持 Unicode。
- 动画和过渡保持短促，不阻塞操作，也尊重系统“减少动态效果”设置。
