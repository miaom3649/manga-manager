# HManガ使用说明

> Windows 安装后有“ HManガ ”和“ HManガ（调试）”两个开始菜单入口。调试入口会额外显示黑色日志窗口，仅用于测试；二者共用资料，请勿同时启动。

## 1. 安装与首次启动

1. 在 Windows 10/11 上运行 `HManga-Setup-*.exe`。
2. 安装后打开“ HManガ ”。
3. 首次启动选择一个作品主目录。软件会自动创建“插画”和“备份”子目录。
4. 把漫画 ZIP 放在主目录第一层；把单张图片放进“插画”目录。软件启动和运行期间都会自动检测变化。

主窗口会记住关闭时的位置和大小，下次启动自动恢复。作品详情使用主窗口内部居中的浮空卡片；阅读器、上传、封面选择和普通确认窗口仍按各自规则打开。

漫画 ZIP 必须未加密且至少包含一张可读取图片。软件将 ZIP 内图片按文件名自然升序排列，并以第一张作为默认封面；详情编辑中可以改选其他图片。只有纯 ASCII 数字 ZIP 文件名会显示只读作品编号，前导零会保留。

## 2. 作品管理

- 顶部搜索框匹配编号、真实文件名、标题、Tag 名和分组名。各类命中结果取并集，英文逗号可分隔多个关键词。
- 左侧可筛选漫画/插画、星级和普通 Tag，并可选择普通 Tag 的任意或全部匹配。漫画与插画互斥且必须选中一个，默认选择漫画。系统与自定义 Tag 连续显示为同样的圆角胶囊按钮；选中时填色，未选中时仅显示边框。Tag 搜索会同时过滤系统与自定义 Tag，例如输入“漫画”不会继续显示“插画”。“管理 Tag”入口位于这一筛选卡片内。
- 每页固定 50 部，支持首页、上一页、下一页、末页和指定页跳转。
- Windows 双击作品条目进入详情，手机单击进入；所有已有作品资料修改都从详情的“编辑”进入。
- 星级为个人评价：1 星“好”、2 星“非常好”、3 星“神作”。
- 点击筛选卡片中的“管理”会直接打开 Tag 管理浮空卡片。分组固定为“作者”和“类别”，新 Tag 默认归入“类别”；删除 Tag 不会删除作品文件。

## 3. 阅读漫画

详情浮空卡片点击“开始阅读”后，软件主窗口会暂时隐藏，只留下阅读器。阅读器返回后恢复主窗口，并重新显示刚才的详情卡片。阅读器提供纵向连续和单页两种模式，设备会记住最后使用的模式。

- 单页模式：Windows 使用鼠标滚轮或方向键翻页，滚轮向上为上一页、向下为下一页；手机点击左侧/右侧翻页。
- 连续模式：自由纵向滚动；与单页模式一样，只在正常退出阅读器时保存最终位置。
- 两种模式均默认将整张图片适配到阅读区域大小，并提供缩放、拖动、恢复适配大小和跳页，不提供全屏按钮。
- 连续滚动时顶部页码会实时跟随当前图片；切换阅读模式时从同一页继续。
- Windows 跳页框不显示上下箭头，可直接输入目标页码。
- Windows 阅读器顶部居中显示标题，左侧箭头返回详情；操作工具栏位于底部。鼠标离开约 5 秒后上下栏一起滑出隐藏，靠近窗口顶部或底部任一侧时两栏一起滑回。
- 在页码框中输入时，上下栏会保持显示；光标离开页码框且鼠标远离边缘 5 秒后才会隐藏。
- 阅读器带有可见边框，可拖动顶部标题栏调整窗口位置，双击标题栏最大化或恢复。
- Windows 阅读器可使用左箭头、Esc 或 Alt+F4 退出。
- 每次进入仍从开头开始；有历史进度时，5 秒内可点击“回到上次观看位置”。
- 该按钮始终跳到打开阅读器之前保存的位置，即使你已临时浏览到更靠后的页面也不会改变目标。
- 翻页、滚动、跳页和切换模式时不会修改历史进度；使用返回按钮、Esc 或 Alt+F4 正常退出时才保存一次。程序或浏览器异常关闭时，本次位置不会保存。

手机进入阅读器后会下载当前漫画用于本次会话。退出阅读器会清理会话图片缓存；断网进度会在重新连接对应电脑后同步。

## 4. 上传

Windows 首页点击“上传”或把文件拖入窗口。手机网页不提供上传功能。

文件会先进入临时区校验。合法且没有重名的文件会直接使用默认标题、空 Tag、未评价和默认封面加入作品库，不打开编辑页面；之后可从详情页编辑资料。

整批中存在不合法文件时，悬浮卡片会列出原因并拒绝整批加入。如果存在同名文件，软件只汇总询问一次“覆盖/取消”；选择覆盖后立即事务式提交，失败时恢复原文件和数据库资料。

## 5. 手机配对

1. Windows 打开“设置 → 手机配对与设备管理”。
2. 确认手机与电脑连接同一个可信的 Wi-Fi。
3. 手机直接打开显示的访问地址并输入六位配对码，或者扫描二维码自动打开页面并填写配对码；确认设备名称后完成连接。
4. 配对凭证会保存在该浏览器中，以后直接打开或收藏电脑地址即可。

配对页面关闭后，原二维码和配对码立即失效。Windows 可撤销任一设备。手机“断开当前电脑”也会撤销本机凭证。不要通过端口映射把 HManガ 暴露到公网；本项目按个人可信局域网使用设计，HTTP 本身不加密。

手机网页沿用电脑端的 HManガ 主题。顶部铃铛打开通知，齿轮打开设置；漏斗按钮打开筛选卡片。作品详情、编辑、Tag 管理和确认提示都在当前页面上方以悬浮卡片显示。

若 Windows 防火墙首次询问网络权限，只允许“专用网络”，不要勾选公共网络。

## 6. 备份、缓存与目录迁移

- 每天第一次启动会自动备份；连续运行时在凌晨 2 点备份，休眠错过后会补做。
- 自动备份只保留最近 5 份；手动备份和“恢复前”保护备份不自动删除。
- 恢复只覆盖管理资料，不修改漫画 ZIP 或插画原图；恢复前必须确认并先创建保护备份。
- 缩略图缓存默认上限 1 GB，也可选择 2 GB、5 GB 或不限。缓存可随时清理并自动重建。
- “更换并迁移作品目录”只迁移数据库已收录的作品。异常文件、未知内容和旧备份保留在原目录；冲突或失败时整次迁移回滚。新目录不能与当前目录相同，但可以选择当前目录内的子目录；这种情况下外层旧目录会保留。
- 设置页底部的红色“恢复所有设置”会在二次确认后重建整个软件数据库，清除标题、Tag、星级、封面、进度、通知、配对和主题等全部管理资料，并删除备份与缓存。漫画、插画原文件和当前作品目录位置会保留，随后重新扫描；重建失败时自动恢复旧数据库。

## 7. 退出与卸载

关闭主窗口只会缩小到系统托盘，手机服务继续运行。左键单击托盘图标会直接显示主页窗口；右键托盘图标才会打开菜单。托盘菜单可用于展示主窗口、直接打开设置或手机配对、切换浅色/深色模式、重启软件或完整退出。有上传任务时，重启和退出都会先按现有规则选择等待完成或取消任务。

软件已在运行时再次点击 Windows 桌面图标，不会启动第二个 HManガ，而是直接显示已运行软件的主页窗口。

卸载程序时可选择是否删除数据库、设置和缓存。卸载不会删除作品主目录中的漫画、插画和备份。

## 8. 故障排查

- 手机打不开：确认 HManガ 仍在窗口或托盘运行、两台设备在同一 Wi-Fi、防火墙允许专用网络，并重新打开配对页核对最新地址。
- ZIP 未收录：确认未加密、复制已经完成，并且压缩包内至少有一张可解码图片。
- 文件显示丢失：点击通知查看列表，检查是否在资源管理器中删除或移动了文件。
- 封面或页面损坏：更换封面或修复原 ZIP；软件不会修改原 ZIP 内容。
- 恢复失败：原数据库保持不变；先在“备份”目录检查所选文件是否为完整的 HManガ SQLite 备份。
