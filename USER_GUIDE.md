# H库使用说明

> Windows 安装后有“ H库 ”和“ H库（调试）”两个开始菜单入口。调试入口会额外显示黑色日志窗口，仅用于测试；二者共用资料，请勿同时启动。

## 1. 安装与首次启动

1. 在 Windows 10/11 上运行 `HLibrary-Setup-*.exe`。
2. 安装后打开“ H库 ”。
3. 首次启动选择一个作品主目录。软件会自动创建“插画”和“备份”子目录。
4. 把漫画 ZIP 放在主目录第一层；把单张图片放进“插画”目录。软件启动和运行期间都会自动检测变化。

漫画 ZIP 必须未加密且至少包含一张可读取图片。软件将 ZIP 内图片按文件名自然升序排列，并以第一张作为默认封面；详情编辑中可以改选其他图片。只有纯 ASCII 数字 ZIP 文件名会显示只读作品编号，前导零会保留。

## 2. 作品管理

- 顶部搜索框匹配编号、真实文件名和标题。英文逗号可分隔多个关键词。
- 左侧可筛选漫画/插画、星级和普通 Tag，并可选择普通 Tag 的任意或全部匹配。
- 每页固定 50 部，支持首页、上一页、下一页、末页和指定页跳转。
- Windows 双击作品条目进入详情，手机单击进入；所有已有作品资料修改都从详情的“编辑”进入。
- 星级为个人评价：1 星“好”、2 星“非常好”、3 星“神作”。
- Tag 管理支持隐藏分组、同组唯一、跨组同名、改名、移动和两种分组删除方式。删除 Tag 不会删除作品文件。

## 3. 阅读漫画

详情页点击“开始阅读”。阅读器提供纵向连续和单页两种模式，设备会记住最后使用的模式。

- 单页模式：Windows 使用鼠标滚轮或方向键翻页，滚轮向上为上一页、向下为下一页；手机点击左侧/右侧翻页。
- 连续模式：自由纵向滚动；与单页模式一样，只在正常退出阅读器时保存最终位置。
- 两种模式均默认将整张图片适配到阅读区域大小，并提供缩放、拖动、恢复适配大小和跳页，不提供全屏按钮。
- 连续滚动时顶部页码会实时跟随当前图片；切换阅读模式时从同一页继续。
- Windows 跳页框不显示上下箭头，可直接输入目标页码。
- Windows 阅读器顶部居中显示标题，左侧箭头返回详情；操作工具栏位于底部。鼠标离开约 5 秒后上下栏一起滑出隐藏，靠近窗口顶部或底部任一侧时两栏一起滑回。
- 在页码框中输入时，上下栏会保持显示；光标离开页码框且鼠标远离边缘 5 秒后才会隐藏。
- 阅读器带有可见边框，可拖动顶部标题栏调整窗口位置，双击标题栏最大化或恢复。
- Windows 阅读器可使用左箭头、Esc 或 Alt+F4 退出。
- 每次进入仍从开头开始；有历史进度时，5 秒内可点击“回到上次观看位置”。
- 该按钮始终跳到打开阅读器之前保存的位置，即使你已临时浏览到更靠后的页面也不会改变目标。
- 翻页、滚动、跳页和切换模式时不会修改历史进度；使用返回按钮、Esc 或 Alt+F4 正常退出时才保存一次。程序或浏览器异常关闭时，本次位置不会保存。

手机进入阅读器后会下载当前漫画用于本次会话。退出阅读器会清理会话图片缓存；断网进度会在重新连接对应电脑后同步。

## 4. 上传

Windows 首页点击“上传”或把文件拖入窗口。手机首页点击“上传”选择多个文件。

文件会先进入临时区校验，不会立即写入作品目录。可以设置公共 Tag，再逐项修改标题、Tag、星级和漫画封面。异常项必须移除或取消整批任务。

如果存在同名文件，软件只汇总询问一次。选择覆盖后仍会停留在资料确认阶段；只有最后点击“确定上传”才会提交。提交失败时会恢复原文件和数据库资料。

## 5. 手机配对

1. Windows 打开“设置 → 手机配对与设备管理”。
2. 确认手机与电脑连接同一个可信的 Wi-Fi。
3. 用手机扫描二维码，在网页中输入 Windows 显示的六位配对码和设备名称。
4. 配对凭证会保存在该浏览器中，以后直接打开或收藏电脑地址即可。

配对页面关闭后，原二维码和配对码立即失效。Windows 可撤销任一设备。手机“断开当前电脑”也会撤销本机凭证。不要通过端口映射把 H库 暴露到公网；本项目按个人可信局域网使用设计，HTTP 本身不加密。

若 Windows 防火墙首次询问网络权限，只允许“专用网络”，不要勾选公共网络。

## 6. 备份、缓存与目录迁移

- 每天第一次启动会自动备份；连续运行时在凌晨 2 点备份，休眠错过后会补做。
- 自动备份只保留最近 5 份；手动备份和“恢复前”保护备份不自动删除。
- 恢复只覆盖管理资料，不修改漫画 ZIP 或插画原图；恢复前必须确认并先创建保护备份。
- 缩略图缓存默认上限 1 GB，也可选择 2 GB、5 GB 或不限。缓存可随时清理并自动重建。
- “更换并迁移作品目录”只迁移数据库已收录的作品。异常文件、未知内容和旧备份保留在原目录；冲突或失败时整次迁移回滚。

## 7. 退出与卸载

关闭主窗口只会缩小到系统托盘，手机服务继续运行。需要彻底退出时使用托盘菜单“退出”。有上传任务时可选择等待完成或取消任务后退出。

卸载程序时可选择是否删除数据库、设置和缓存。卸载不会删除作品主目录中的漫画、插画和备份。

## 8. 故障排查

- 手机打不开：确认 H库 仍在窗口或托盘运行、两台设备在同一 Wi-Fi、防火墙允许专用网络，并重新打开配对页核对最新地址。
- ZIP 未收录：确认未加密、复制已经完成，并且压缩包内至少有一张可解码图片。
- 文件显示丢失：点击通知查看列表，检查是否在资源管理器中删除或移动了文件。
- 封面或页面损坏：更换封面或修复原 ZIP；软件不会修改原 ZIP 内容。
- 恢复失败：原数据库保持不变；先在“备份”目录检查所选文件是否为完整的 H库 SQLite 备份。
