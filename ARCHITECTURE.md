# HManガ技术架构

## 1. 架构结论

程序显示名称统一为 `HManガ`，代码包、命令、数据目录、数据库文件和网页存储键等内部标识统一为小写 `hmanga`。程序不再识别或迁移旧版 `HLibrary`/`hlibrary` 数据、中文目录名、旧备份名和旧网页存储键。

HManガ采用一个 Windows 主程序承载全部核心能力：

```text
HManガ Windows 主程序
├── PySide6 原生桌面界面
├── 业务服务层
├── SQLite 数据库
├── 文件扫描与目录监控
├── ZIP/图片读取与缩略图缓存
├── 后台任务与通知
└── FastAPI 局域网服务
      └── 手机响应式网页（PWA）
```

- Windows 桌面端是真正的原生应用，不依赖浏览器操作本机功能。
- 手机网页只通过局域网 API 使用 Windows 主程序提供的数据和媒体。
- 桌面界面和手机 API 复用同一业务服务层，避免出现两套规则。
- 不引入云服务，不要求公网账号，不进行跨电脑数据同步。

## 2. 推荐技术栈

### Windows 主程序

- Python 3.12。
- PySide6：窗口、系统托盘、原生文件选择器、桌面阅读器。
- FastAPI + Uvicorn：手机局域网 API 和静态网页服务。
- SQLAlchemy 2：数据库访问和事务。
- Alembic：数据库结构升级。
- SQLite（WAL 模式）：本地数据存储。
- watchdog：Windows 目录变化监听。
- Pillow：常规图片解码和缩略图。
- pillow-heif：HEIC/HEIF/AVIF 解码补充。
- Python `zipfile`：ZIP 枚举和按需读取。
- `blake3` 或 SHA-256：文件内容指纹；首版优先使用标准库 SHA-256，后续再按性能决定是否更换。
- `qrcode`：生成配对二维码。

### 手机网页

- TypeScript。
- React + Vite。
- React Router：作品、详情、阅读器、通知和设置导航。
- TanStack Query：API 请求、缓存和失效刷新。
- IndexedDB：配对凭证、设备偏好、临时漫画页面和离线进度。
- Service Worker：PWA 外壳和当前阅读会话临时缓存。
- CSS 响应式布局；不依赖云端 CDN，所有资源由 Windows 主程序提供。

### 界面文案

- 桌面端和手机端共用 `src/hmanga/locales/zh-CN.json` 作为唯一的中文文案目录。
- 所有语言包统一使用英文标识键；完整动态模板使用 `works.count` 这类语义键，普通静态文案使用 `action.*`、`label.*`、`error.*`、`confirm.*`、`status.*` 或 `message.*` 稳定标识。中文只存在右侧显示值中。
- 设置页根据用户数据目录下 `locales/*.json` 自动生成语言选项；手动放入 `en-US.json` 并重启后即可选择。语言包可仅包含需要覆盖的键，缺失项回退到简体中文。
- 手机端通过已配对 API 读取可用语言列表和合并后的语言包；手机语言独立保存在浏览器 `hmanga-language` 键中，不与电脑端语言联动。
- 设置页在“外观主题”下依次显示主题和语言两个下拉框，不使用分组边框，整体位于缩略图缓存设置上方。电脑主题可选跟随系统、浅色或深色；跟随系统时会监听操作系统深浅色变化并立即更新。浅色主题使用降低明度的灰粉背景，Tag 胶囊在浅色主题下使用深色文字保持对比度；深色主题的 Tag 配色保持不变。
- 动态文案中的文件名、数量和日期保持由程序插入，其固定文案片段仍从同一 JSON 读取。

### 打包与安装

- PyInstaller `onedir` 模式打包 Windows 主程序；相比单文件模式，启动更快，也更适合包含 Qt、网页资源和图片解码器。
- Inno Setup 生成标准 Windows 安装包、开始菜单、可选桌面快捷方式和卸载入口。
- GitHub Actions Windows Runner 执行测试、PyInstaller 构建和 Inno Setup 安装包构建。
- Ubuntu 负责日常开发；Windows CI 负责持续验证；真实 Windows 10/11 完成最终验收。

## 3. 进程和线程模型

首版保持单进程，减少安装、端口和生命周期复杂度：

- Qt 主线程：只处理桌面 UI 和轻量事件。
- API 线程：运行 Uvicorn 事件循环。
- 后台任务池：扫描、哈希、ZIP 校验、缩略图、备份、上传提交和目录迁移。
- SQLite 单写入队列：所有修改统一串行提交，读取可并发，降低 `database is locked` 风险。
- Qt 信号将业务事件推回桌面 UI；手机端每 0.5 秒短轮询轻量状态接口获取资料版本和通知数量，不使用 Server-Sent Events 长连接。

不得在 Qt 主线程执行 ZIP 解码、全文件哈希、目录迁移或网络上传。

## 4. 分层设计

```text
UI 层
├── Desktop UI（PySide6）
└── Mobile Web（React）
        ↓
Application Services
├── LibraryService
├── SearchService
├── MetadataService
├── TagService
├── ReaderService
├── UploadService
├── NotificationService
├── PairingService
├── BackupService
└── MigrationService
        ↓
Infrastructure
├── SQLite repositories
├── File watcher/scanner
├── ZIP and image codecs
├── Thumbnail/cache store
├── HTTP streaming
└── Windows integration
```

业务规则必须位于 Application Services，不能只写在某个界面中。例如删除 Tag 的影响统计、同名覆盖事务、替换文件待确认，都必须由桌面和手机共用。

## 5. 数据模型

### `works`

- `id`：内部稳定 UUID，不使用文件名作主键。
- `kind`：`comic` 或 `illustration`；界面映射为只读系统 Tag“漫画/插画”。
- `relative_path`：相对主目录路径。
- `file_name`：带扩展名真实文件名。
- `number`：纯数字 ZIP 的编号文本，可空，保留前导零。
- `title`：用户标题，可空；显示时回退到去扩展名文件名。
- `rating`：0～3。
- `cover_member`：漫画封面在 ZIP 内的路径；插画为空。
- `fingerprint`：内容指纹。
- `file_size`、`modified_at`。
- `status`：`ready`、`replacement_pending`、`invalid` 等。
- `added_at`、`updated_at`。

### `tag_groups`

- `id`、`name`、`normalized_name`。
- 分组名全局唯一。
- 分组是隐藏属性，不存储颜色。

### `tags`

- `id`、`name`、`normalized_name`、`group_id`（可空）。
- 同一分组内名称唯一；不同分组可重名。
- 系统类型不存入此表，避免被普通 Tag 操作误删；API 将 `works.kind` 映射成两个虚拟系统 Tag。

### `work_tags`

- `work_id`、`tag_id` 联合唯一。

### `reading_progress`

- `work_id` 唯一。
- `page_index`、`page_offset`、`content_fingerprint`、`updated_at`。
- 所有设备共享一份最新进度；离线同步取更靠后位置。

### `devices`

- `id`、自定义名称、设备/浏览器信息、凭证哈希、首次配对时间、最近访问时间、撤销时间。桌面配对页以固定分隔行显示设备名，点击后显示配对时间、最近访问时间和可识别的手机型号，并可删除设备。
- 数据库只保存访问凭证哈希，不保存可直接使用的明文凭证。

### `device_preferences`

- `device_id`、主题、阅读模式、排序字段、排序方向。
- Windows 本机使用固定的本地设备 ID。

### `notifications`

- `id`、类型、标题、结构化详情 JSON、创建时间、已读时间、过期时间。
- 所有配对设备共享已读状态。

### `file_observations`

- 记录异常文件和最近观察到的大小、修改时间及指纹，避免重复通知。

### `cache_entries`

- 缓存键、文件位置、尺寸、最后访问时间、是否正在使用。
- 支持 1GB/2GB/5GB/不限和 LRU 清理。

### `jobs`

- 扫描、上传、迁移、备份等后台任务的状态和进度。
- 重要任务崩溃恢复时可判断临时目录是否需要清理或回滚。

## 6. 文件扫描与身份识别

### 扫描范围

- 主目录第一层：有效、未加密、含可读图片的 ZIP → 漫画。
- `illustration` 子目录第一层：可成功解码的单张图片 → 插画。
- `config-backup`、其他子目录和其他文件忽略。升级时会自动将旧 `插画` 和 `备份` 目录迁移为英文名。

### 稳定身份

- 运行期间优先使用 watchdog 提供的创建、删除、修改、重命名事件。
- 每部作品使用内部 UUID，不让文件名变化破坏资料关联。
- 启动扫描时先以相对路径匹配，再用尺寸、修改时间和内容指纹判断重命名、替换或新增。
- 内容指纹只在新文件或可疑变化时计算，避免每次启动重新读取全部 2.7GB/未来大库。
- 单纯修改时间变化但内容指纹一致时，只更新观察数据。
- 同路径内容指纹变化时进入 `replacement_pending`，等待用户选择。

### 事件合并

Windows 文件复制可能产生多次变化事件。监听器必须防抖，并等待文件尺寸稳定且可以独占/共享读取后再校验，避免扫描半个 ZIP。

## 7. 搜索与分页

- SQLite 为规范化文件名、规范化标题、添加时间、评分、类型建立索引。
- Unicode 规范化在写入时生成搜索列；英文大小写折叠。
- 逗号批量关键词拆分后采用 OR；文本条件与类型、星级、普通 Tag 组之间采用 AND。
- 普通 Tag 组内部由用户选择 ANY/ALL。
- 固定每页 50 条，使用稳定的第二排序键 `works.id` 防止翻页重复或遗漏。
- 首页/上一页/下一页/末页和指定页跳转需要总数查询；10,000 部目标规模下 SQLite 足够。

## 8. 媒体与缓存

### 漫画

- ZIP 内图片列表按自然文件名排序。
- ZIP 内可读图片按成员文件名自然升序排列，第一张为默认封面；自定义封面仅保存成员路径。
- 预览候选取排序结果中的第 4、7、10、13、16 张，最多五张，不依赖固定文件名或前导零位数。
- 阅读时按需读取 ZIP 成员，不永久解压漫画目录。

### 插画

- 先尝试实际解码，不只信任扩展名。
- 静态缩略图转为适合 UI 的 WebP/JPEG 缓存。
- 动图生成限尺寸动态预览；离屏或非当前页停止动画解码。

### 缓存键

缓存键至少包含作品内容指纹、ZIP 成员路径、目标尺寸、主题无关的变换版本。文件替换或更改封面后不会错误复用旧缓存。

## 9. 上传事务

```text
选择文件
→ 分块上传到任务临时目录
→ 校验格式/内容/空间/同名冲突
→ 用户编辑元数据
→ 最终确认
→ 保护旧文件和数据库快照
→ 提交全部文件与数据库
→ 校验
→ 清理保护副本和临时目录
```

- 手机上传使用分块传输，支持进度和安全取消；浏览器页面关闭时任务进入取消或等待超时清理。
- 同名冲突在整批预检后只询问一次。
- 最终提交必须全有或全无；任何失败都回滚已覆盖文件和数据库事务。
- 不合格项不能进入最终提交。
- 临时目录使用任务 UUID，启动时清理已确认废弃的残留任务。

## 10. 主目录迁移事务

- 先预检全部目标路径、冲突、权限和可用空间。
- 任意冲突则不开始迁移。
- 同盘移动也记录迁移日志；跨盘逐文件复制、校验后暂不删除源文件。
- 全部目标校验成功后，在一个数据库事务内切换根路径和相对路径。
- 数据库切换成功后再清理旧作品文件。
- 中途取消或失败时根据迁移日志回滚到完整可用的一侧。
- 只迁移已收录作品；旧备份和异常内容留在原处。

## 11. 手机配对与安全

- API 只绑定局域网可达地址；不实现 UPnP、端口映射或公网发布。
- 配对页面打开时创建随机配对会话；关闭页面立即作废。
- 六位配对码是当前配对页面唯一的临时配对凭证；二维码只包含局域网地址和该配对码，用于打开手机页面并自动填写，不包含长期设备凭证。每成功配对一台设备即轮换临时码，桌面浮空卡定时同步临时码和设备名单，并保留按客户端限制失败次数的防暴力尝试保护。
- 配对成功后只向该手机签发一次长期随机凭证；Windows 只保存哈希。
- 后续请求使用 Authorization 头或安全等效机制，敏感写操作校验设备权限。
- HTTP 不提供传输加密，因此 UI 明确提示只在可信私人 Wi-Fi 使用。
- 后端强制限制手机不能迁移目录、恢复备份、管理其他设备或退出服务。
- 对配对尝试、认证失败和上传接口做速率限制。

## 12. 手机临时离线阅读

- 打开漫画后分批把页面写入 HManガ 自己的 IndexedDB/Cache Storage；前几页完成即可阅读，余下页面继续下载。
- 退出阅读器时删除当前漫画所有页面缓存。
- 启动网页时清理崩溃遗留的阅读会话缓存。
- 空间不足时先清理 HManガ 自有旧缓存并重试一次；再次失败后降级为在线逐页读取。
- 离线进度单独保存，不随图片缓存删除；重连原 Windows 实例后同步。
- 离线进度绑定内容指纹；冲突取更靠后的位置，内容已替换时丢弃旧进度并通知。

## 13. 备份与恢复

- 备份 SQLite 一致性快照和必要设置，不复制媒体和可重新生成缓存。
- 备份文件统一保存在 `config-backup` 目录中，文件系统路径不使用中文。
- SQLite 在线备份 API 或等效一致性机制，不能直接复制正在写入的数据库文件。
- 备份文件名为 `hmanga-auto-YYYYMMDD-HHMMSS.sqlite`、`hmanga-manual-YYYYMMDD-HHMMSS.sqlite` 或 `hmanga-restore-YYYYMMDD-HHMMSS.sqlite`，不使用中文和随机后缀。
- “恢复所有设置”会重建当前管理数据库并清理缓存，但不删除备份目录中的任何已有备份。
- 恢复前校验格式、版本和完整性，再创建恢复前保护备份。
- 恢复后运行数据库迁移和文件重扫，以当前磁盘状态校正记录。
- 恢复配对凭证后应重新加载撤销状态；安全上可在后续评估是否要求设备重新配对。

## 14. 错误恢复原则

- 原作品文件优先于缓存和索引。
- 任何覆盖、迁移或恢复都先验证后删除旧数据。
- 数据库修改使用事务。
- 可恢复任务保留日志；不可恢复临时数据在启动时安全清理。
- 失败通知说明受影响文件和下一步，不只显示“发生错误”。

## 15. Windows 生命周期

- 用户手动启动，不注册开机自启动。
- 关闭主窗口进入托盘，API、监控和后台任务继续。
- 桌面端使用 Qt 本地 IPC 保证单实例运行。再次点击 Windows 桌面图标时，新进程只通知已有进程显示主页后立即退出，不再打开数据库或绑定 API 端口。
- 桌面端检测到共享数据版本变化时，同一次刷新作品列表、左侧 Tag 筛选栏和通知；从作品编辑页创建、编辑或删除 Tag 后，回到主页必须立即显示最新 Tag。
- 托盘图标根据未读状态切换普通/红点版本。
- 彻底退出时停止接受新任务；有活动任务则选择等待完成或安全取消。
- 退出顺序：停止 API 接入 → 停止监听 → 处理任务 → 刷新数据库 → 关闭托盘。重启时必须等待 API 线程完全结束、端口释放后才启动唯一的新进程；重复点击重启不得创建多个进程。

## 16. 性能目标

- 当前基准：146 部、2.7GB。
- 设计目标：10,000 部仍可正常搜索、筛选和分页。
- 首页首次结果应优先显示文本信息，封面异步补齐。
- Qt 主线程不得出现超过 100ms 的可避免阻塞。
- 常用搜索在数据库热缓存下目标低于 200ms；手机局域网不含图片的 API 目标低于 300ms。
- 阅读器只保留当前页面附近的解码图，避免全部原图常驻内存。

## 17. 关键验证

- 中文、日文、emoji、前导零、全角/半角搜索。
- ZIP 损坏、加密、复制未完成、同名覆盖、外部替换、外部重命名。
- 不同盘符迁移、中断、空间不足、权限失败和回滚。
- Windows 关闭到托盘、休眠唤醒、跨日备份、退出等待/取消。
- iPhone Safari、Android Chrome、IP 变化、网络中断、离线进度同步。
- 10,000 条分页、复杂 Tag ANY/ALL、评分筛选和稳定排序。
- 浅色/深色、GIF 当前页动画、缩略图清理与重新生成。
